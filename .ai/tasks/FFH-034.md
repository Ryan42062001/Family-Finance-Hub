# FFH-034 — Foundation CI Documentation Fast Path + Evidence

Schema: FFH_TASK_V1
Owner: Product & Technical R&D Engineer
State: ACTIVE
Execution mode: STANDARD_CHAT_HIGH
Dependency classification: INDEPENDENT CONTROL-PLANE / CI IMPLEMENTATION — FFH-018 discovery accepted; does not depend on FFH-020/FFH-016
Approved integration base: current `phase-5-money-priority-engine` Manager authorization checkpoint
Task branch: `ffh/ffh-034-ci-efficiency-observability`
Activation: USER_AUTHORIZED
Blocked by: NONE
Next owner: Product & Technical R&D Engineer
MANAGER_VERDICT: PENDING
AUDIT_STATUS: REQUIRED — material CI/control-plane change requires fresh independent Technical / Workflow audit after Manager integration and live fast-path proof

## Objective

Implement the Manager-accepted FFH-018 architecture so documentation-only pull requests retain one trustworthy `Foundation CI / verify` status while skipping only expensive validation, all ambiguous/non-doc/manual-dispatch cases remain full-validation, and every run emits durable SHA/path/test evidence.

## Strict implementation scope

Production implementation files are limited to:
- `.github/workflows/ci.yml`;
- `scripts/ci-change-mode.mjs`;
- `scripts/ci-change-mode.test.mjs`.

Task/handoff/audit documentation may also change as control-plane evidence.

Do not modify `package.json`, lockfiles, application/source calculation code, tests outside the classifier test, Supabase files, migrations, live data, financial policy, financial formulas, or Phase-6 code without a new Manager authorization.

## Required workflow invariants

Preserve:
- workflow name exactly `Foundation CI`;
- job id exactly `verify`;
- PR triggers for `main` and `phase-5-money-priority-engine`;
- `workflow_dispatch`;
- self-hosted Windows/X64/`ffh-local` runner;
- Node 22;
- existing placeholder Supabase CI environment;
- existing FULL validation stage semantics and order:
  1. install dependencies;
  2. AI-state validation;
  3. production dependency audit;
  4. calculations;
  5. security contract;
  6. typecheck;
  7. lint;
  8. build.

The verify job MUST always be created. Do not add workflow-level `paths-ignore`, path filters that suppress the workflow, or job-level `if` conditions that can remove `verify`.

## Fail-closed change classification

For pull requests, classify from the PR base/head diff using rename-safe/deletion-safe evidence. Prefer a no-renames, status-aware diff so a source-file rename into an allowed docs path cannot hide the original non-doc path.

The only DOCS_ONLY paths are:
- any Markdown file under `.ai/`;
- any Markdown file under `docs/`;
- any root-level Markdown file.

Anything else => FULL.

FULL is mandatory for:
- workflow files;
- package/lock files;
- scripts;
- source/application files;
- calculation/security tests;
- migrations/Supabase;
- configuration;
- mixed docs + any non-doc path;
- deletion or rename involving any non-doc path;
- empty, missing, malformed, ambiguous, or unparseable change evidence;
- classifier failure;
- unsupported event;
- `workflow_dispatch` unconditionally.

## Cheap path

Every run still performs:
- checkout;
- Node setup;
- classifier execution;
- classifier unit tests;
- AI-state validation;
- evidence/summary generation;
- evidence artifact upload.

DOCS_ONLY may skip only:
- `npm ci`;
- production dependency audit;
- calculation tests;
- security tests;
- typecheck;
- lint;
- build.

AI-state validation must continue to run on documentation-only changes so `.ai/**/*.md` state errors cannot bypass machine validation.

## Full path

For FULL mode, run every existing Foundation CI stage with the same failure semantics and stage ordering. The implementation PR itself contains non-doc workflow/script changes and therefore MUST exercise FULL mode.

No classifier outcome may convert a non-doc or ambiguous run into DOCS_ONLY.

## Durable observability

Every verify run must expose in `GITHUB_STEP_SUMMARY` and a small durable evidence artifact:
- classification: FULL or DOCS_ONLY;
- classification reason;
- event name;
- PR number when present;
- PR base SHA and head SHA when present;
- actual workflow-tested SHA (`github.sha`, including the PR merge SHA when applicable);
- changed path/status evidence;
- workflow run id/number and verify job identity;
- which validation stages executed versus were intentionally skipped.

For FULL runs, capture executed test/validation output into the evidence artifact while preserving console visibility and exact command exit/failure semantics.

Use `actions/upload-artifact@v4` with `if: always()` so evidence survives failures. Keep retention bounded (target 14 days unless live repository policy requires a smaller supported value). Do not upload secrets, environment values, Supabase credentials, user financial data, node_modules, build output, or repository contents unrelated to CI evidence.

## Classifier adversaries required

Direct unit tests must prove:
- root `README.md` => DOCS_ONLY;
- `.ai/tasks/example.md` => DOCS_ONLY;
- `docs/example.md` => DOCS_ONLY;
- `.ai/example.yml` => FULL;
- `.github/workflows/ci.yml` => FULL;
- `package.json` / lockfile => FULL;
- `scripts/example.mjs` => FULL;
- `lib/example.ts` => FULL;
- `tests/example.test.ts` => FULL;
- `supabase/migrations/example.sql` => FULL;
- docs + one non-doc => FULL;
- deletion of non-doc => FULL;
- rename from non-doc into docs => FULL;
- malformed/unknown status => FULL;
- empty change evidence => FULL;
- classifier error => FULL;
- manual dispatch => FULL.

## Validation / custody plan

Worker returns READY_FOR_MANAGER unmerged with:
- exact branch/head SHA;
- changed-file scope;
- classifier tests;
- implementation PR targeting `phase-5-money-priority-engine`;
- FULL Foundation CI on the implementation PR;
- proof the check identity remains `Foundation CI / verify`;
- summary/artifact evidence showing base/head/tested SHA and full-stage outputs;
- no financial/Supabase/live-data/package/lock changes;
- handoff SHA.

Manager acceptance is not closure.

After Manager integrates the accepted implementation:
1. verify integration FULL CI on the exact integration SHA;
2. create a disposable docs-only validation PR against the now-integrated milestone;
3. prove that the same `Foundation CI / verify` job exists and succeeds in DOCS_ONLY mode;
4. prove classifier tests + AI-state validation + evidence upload ran;
5. prove expensive steps were intentionally skipped;
6. close the disposable validation PR without merging unless its docs change is independently useful;
7. freeze the exact integrated workflow target;
8. require one fresh independent Technical / Workflow audit under Workflow V3.1 material-control-plane rules.

FFH-034 may reach CLOSED only after the independent workflow audit passes or passes with acceptable non-blocking findings and Manager reconciles them.

## Protected boundaries

Do not:
- weaken financial-engine validation;
- change any financial behavior or policy;
- alter Supabase/schema/live data;
- alter FFH-020 or FFH-016 authority/gates;
- make PR #5 merge-ready;
- activate Phase 6;
- enable/disable branch protection or rulesets;
- require a GitHub plan upgrade;
- reuse the stale `manager/ffh-018-ci-efficiency-observability` branch;
- merge/self-accept/self-audit.

## Checkpoints

PRODUCTION_SHA: Not yet established — CI/control-plane implementation
VALIDATED_CI: Not yet established
HANDOFF_SHA: Not yet established
INTEGRATION_SHA: Not yet established
