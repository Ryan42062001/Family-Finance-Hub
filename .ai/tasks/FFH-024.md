# FFH-024 — Supabase Migration-History Recovery Plan

Owner: Product & Technical R&D Engineer
State: ACTIVE
Dependency classification: blocks Manager authorization to resume FFH-020 live deployment
Approved integration base: current `phase-5-money-priority-engine`
Execution mode: WORK_MODE_PREFERRED
Fallback: normal chat with repository + current official Supabase documentation is valid.

## Objective
Produce an evidence-backed, lowest-risk migration-history reconciliation plan for the linked Supabase project before any further FFH-020 live writes.

## Verified starting problems
1. Repository migration `20260902190000_phase_5a_hybrid_retirement_floor.sql` is absent from live migration history and its expected `household_financial_preferences.expected_hsa_medical_spending_annual` column is absent live.
2. Repository migration `20260903134156_phase_5b_goal_intelligence.sql` differs from live history entry `20260903135253 phase_5b_goal_intelligence`, while representative Phase 5B schema is already present live.
3. The earliest repository file/version and live history also differ (`0001_household_foundation.sql` vs `20260829180242 household_foundation`).
4. FFH-010 `20260909005000` and FFH-011 `20260909033000` are both still absent live.
5. Current connected Supabase MCP `apply_migration` does not expose a repository version argument; using it may generate new remote-only version identities.

## Required research
Use current authoritative Supabase documentation/tool semantics and actual repository/live evidence where available. Determine:
- supported semantics and risks of `supabase migration list`, `db push --dry-run`, `db push`, `migration repair`, and any current equivalents;
- how to reconcile a migration whose schema is already live under a different recorded version;
- how to handle a genuinely pending earlier migration that is missing both history and schema;
- whether migration-history repair can safely map repository `20260903134156` to the already-live Phase 5B schema without reapplying DDL;
- whether/when the missing Phase 5A migration should be applied before FFH-010/011;
- how to preserve exact repository versions for FFH-010/011;
- whether the old `0001` mismatch needs active repair now or can remain documented historical drift;
- prerequisites such as backup, CLI authentication, database password/access token, linked-project configuration, or user action;
- rollback/recovery expectations if a reconciliation/deployment step fails;
- which exact actions are safe for Implementation Engineer versus Work Helper versus actions requiring explicit Manager/user approval.

## Safety
DIAGNOSIS / RECOVERY-PLAN ONLY.
Do not modify live Supabase schema, data, migration history, repository migration SQL, credentials, or production code.
Do not run `migration repair`, `db push`, manual DDL, or direct `supabase_migrations` writes.

## Deliverable
Return `READY_FOR_MANAGER` with:
- verified facts vs assumptions;
- current official-source citations/evidence;
- exact recommended command/action sequence;
- preflight checks;
- whether Phase 5A must deploy first;
- exact Phase 5B history reconciliation strategy;
- treatment of the historical `0001` mismatch;
- exact-version strategy for FFH-010/011;
- required credentials/user actions;
- failure/rollback plan;
- recommended executing role.

If no safe supported reconciliation path can be established, return `BLOCKED` with the exact unresolved capability or authority gap.
