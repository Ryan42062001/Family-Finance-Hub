# FFH-046 — Windfall Tax-Authority Fail-Closed Remediation

Schema: FFH_TASK_V1
Owner: Core Financial Engine Engineer
State: READY_FOR_MANAGER
Execution mode: STANDARD_CHAT_HIGH
Refresh mode: FAST_REFRESH
Dependency classification: HARD DEPENDENCY — blocking FFH-045-P01 remediation
Approved production integration base: `8f4b1c443684446cdf9b619bd35336f5873265bc`
Assigned branch: `ffh/ffh-046-windfall-tax-authority-fail-closed`
Activation: READY
Blocked by: NONE for worker completion — Manager acceptance/integration remains the next gate
Next owner: Manager / Architect

## Objective

Remediate the blocking final Phase-6 policy finding FFH-045-P01 without introducing new tax policy.

The authoritative defect is in `lib/calculations/money-priority-windfall.ts::allocateWindfall()`:
- runtime `taxTreatment` membership is not validated;
- `known_taxable_liability_provided` does not require an explicit liability;
- null/missing liability is coerced through `?? 0`, which can make the otherwise-unreserved remainder deployable;
- unsupported non-null treatment values bypass the current uncertain-tax hold.

## Required behavior

1. Accept only the existing `WindfallTaxTreatment` values:
   - `known_non_taxable`
   - `known_taxable_liability_provided`
   - `uncertain`
   - `not_applicable`
   - existing null/omitted treatment continues to mean uncertain.
2. Unsupported runtime values fail closed with no deployable amount and no allocations.
3. `known_taxable_liability_provided` requires an explicit finite nonnegative `knownTaxLiability`.
4. Missing/null liability must never become an authoritative known-zero tax fact. Preserve existing unknown-safe semantics: the otherwise-unreserved remainder must not deploy; use the existing more-information-needed / tax-review hold behavior unless an existing accepted validator contract requires a stricter invalid response.
5. Explicit `knownTaxLiability: 0` remains a distinct valid supplied fact.
6. Existing explicit known-liability arithmetic, uncertain-tax hold, reservation ordering, retirement-capacity consumption, residual reconciliation, and no-reuse behavior remain unchanged.
7. The authenticated Scenario Lab path must inherit the same fail-closed behavior through the accepted FFH-042/043 adapter chain. Do not create a second Windfall validator/evaluator in App/Data unless a concrete integration defect requires it.
8. No new tax percentages, tax inference, statutory interpretation, schema/persistence/profile write, UI redesign, FFH-038, FFH-047, or Phase 7 work.

## Required regressions

At minimum:
- direct allocator: `known_taxable_liability_provided` + missing liability => no deployable remainder / no allocations and explicit unresolved or invalid status;
- direct allocator: same with `null`;
- direct allocator: explicit zero liability remains valid and distinguishable from missing/null;
- direct allocator: unsupported treatment string fails closed with zero deployable and no allocations;
- direct allocator: explicit known positive liability preserves exact accepted result;
- direct allocator: `uncertain` preserves full otherwise-unreserved tax-review hold;
- Scenario Lab specialized execution: missing/null known liability cannot produce deployable Windfall allocations;
- Scenario Lab specialized execution: unsupported treatment returns a structured non-valid result and no allocations;
- existing FFH-042/043 Windfall reconciliation remains exact:
  `gross = reservations + heldForTaxReview + totalAllocated + remainingUnallocated`;
- full calculation/security/typecheck/lint/build/Foundation CI.

## Financial Engine Reconciliation Gate

Applicability: REQUIRED.

Prove exact cent-level conservation for every new adversary. Arithmetic balance alone is not sufficient: the starting deployable amount must also be authorized by accepted tax-treatment state. No epsilon/tolerance or hidden residual clamp.

## Acceptance criteria

1. FFH-045-P01 is closed at the authoritative allocator boundary.
2. Missing tax authority cannot be converted to known zero.
3. Unsupported runtime tax treatment cannot create deployable cash.
4. Explicit zero liability remains valid.
5. Valid/uncertain Windfall behavior and exact reconciliation remain unchanged.
6. Authenticated specialized Scenario Lab inherits the same behavior.
7. No unrelated production scope changes.
8. FULL Foundation CI passes.
9. Worker returns READY_FOR_MANAGER with exact production/final-validation/handoff SHAs, CI run/job, changed-file scope, reconciliation evidence, and full 11-role dashboard.

## Manager Integration Record

PRODUCTION_SHA: `3a045c4acae7b32efe68165c021dfc34c1a1209a`
VALIDATED_CI: Foundation CI run `35443929743`, job `105899505669` — SUCCESS / FULL
HANDOFF_SHA: This final documentation/control-plane commit; exact SHA returned after creation
INTEGRATION_SHA: not yet established
MANAGER_VERDICT: PENDING
AUDIT_STATUS: NOT_READY — Manager acceptance/integration and a newly frozen remediated Phase-6 target are required before fresh final dual re-audits

## Next action

Core Financial Engine Engineer has completed the bounded FFH-046 remediation on draft PR #62 and returns READY_FOR_MANAGER. Manager independently reviews/accepts/integrates or routes further remediation. Only after accepted integration may Manager freeze a new remediated Phase-6 target and route fresh final dual re-audits; Phase 6 remains open until those gates clear.


## Worker completion evidence

Status: READY_FOR_MANAGER

Pull request:
- #62 — `FFH-046: Fail closed on Windfall tax authority`
- draft / open / unmerged / mergeable

Exact checkpoints:
- PRODUCTION_SHA: `3a045c4acae7b32efe68165c021dfc34c1a1209a`
- FINAL_VALIDATION_SHA: `c5d1b6863707460f046dfd1fc1cf2d0aea82d88c`
- FULL Foundation CI: run `35443929743`, job `105899505669` — SUCCESS
- HANDOFF_SHA: final documentation/control-plane commit after this checkpoint

Production custody:
- `money-priority-windfall.ts` blob is byte-identical from production SHA through final validation.
- Changes after production are test-only typing/reconciliation assertions.

### FFH-045-P01 remediation

The authoritative allocator now:
- accepts only the existing Windfall tax-treatment enum values at runtime;
- preserves null/omitted treatment as existing uncertain-tax behavior;
- returns fail-closed invalid for unsupported non-null treatment, with zero deployable amount and zero allocations;
- requires an explicit finite nonnegative liability for `known_taxable_liability_provided`;
- treats missing/null liability as unresolved tax authority rather than known zero;
- holds all otherwise-unreserved proceeds for tax review when that required liability is missing/null;
- preserves explicit `knownTaxLiability: 0` as a distinct valid supplied fact;
- leaves accepted reservation ordering, post-engine retirement-capacity use, allocation ordering, residual behavior, and no-reuse unchanged.

No second App/Data Windfall evaluator was introduced. The authenticated FFH-042/043 specialized path delegates to the same allocator and inherits the fail-closed result.

### Direct and authenticated regressions

Direct allocator coverage proves:
- omitted known liability -> `more_information_needed`, no deployable amount, no allocations;
- null known liability -> same fail-closed hold;
- explicit zero -> valid and deployable under the explicitly supplied known-zero fact;
- unsupported runtime treatment -> invalid, no deployable amount, no allocations;
- explicit positive known liability -> accepted arithmetic unchanged;
- explicit uncertain treatment -> existing full tax-review hold unchanged;
- every new tax-authority adversary reconciles in exact integer cents.

Authenticated specialized coverage exercises the same missing, null, explicit-zero, unsupported, explicit-positive, and uncertain cases through `executeSpecializedScenarioRun` and verifies the same result semantics and exact-cent identity.

### Exact reconciliation evidence

Omitted-liability adversary:
- gross = `$12,345.67`;
- known other liability = `$200.00`;
- restricted = `$300.03`;
- earmarked = `$400.04`;
- known tax liability authority = missing, so reserved tax is not treated as authoritative zero for deployment;
- held for tax review = `$11,445.60`;
- deployable = `$0.00`;
- allocated = `$0.00`;
- remaining unallocated deployable = `$0.00`;
- exact identity: `$200.00 + $300.03 + $400.04 + $11,445.60 = $12,345.67`.

Unsupported-treatment adversary:
- gross = `$10,000.00`;
- held for tax review = `$10,000.00`;
- deployable / allocated / remaining deployable = `$0.00`;
- state = invalid;
- exact identity remains `$10,000.00 = $10,000.00 held`.

Explicit-zero control:
- gross = `$10,000.00`;
- explicit known tax liability = `$0.00`;
- held = `$0.00`;
- authorized deployable = `$10,000.00`;
- direct control has no eligible destination, so allocated = `$0.00`, remaining unallocated = `$10,000.00`;
- exact identity = `$10,000.00`.

Explicit-positive control:
- gross = `$12,345.67`;
- known tax = `$1,000.01`;
- other liability = `$200.00`;
- restricted = `$300.03`;
- earmarked = `$400.04`;
- reservations = `$1,900.08`;
- authorized deployable = `$10,445.59`;
- direct control has no eligible destination, so allocated = `$0.00`, remaining unallocated = `$10,445.59`;
- exact identity: `$1,900.08 + $10,445.59 = $12,345.67`.

Uncertain-tax control preserves the existing rule: after any explicit reservations, the entire otherwise-unreserved remainder is held for review and no percentage is invented.

### Validation history

- `3a045c4acae7b32efe68165c021dfc34c1a1209a`: production + regression candidate. Calculations/security passed; Foundation CI `35443682750` exposed a test-only TypeScript inference issue.
- `c57c3c3ba12751da3527741f6f0fb503a05c8118`: test-only typing repair; FULL Foundation CI `35443773684` SUCCESS.
- `c5d1b6863707460f046dfd1fc1cf2d0aea82d88c`: final test-only exact-reconciliation strengthening; FULL Foundation CI `35443929743` / job `105899505669` SUCCESS.

Final FULL CI stages:
- AI-state validation PASS;
- dependency audit PASS;
- calculation suite PASS;
- security suite PASS;
- typecheck PASS;
- lint PASS;
- production build PASS;
- Foundation guardrails PASS.

### Bounded changed-file scope before final handoff docs

Production/test:
- `lib/calculations/money-priority-windfall.ts`
- `lib/calculations/money-priority-windfall.test.ts`
- `lib/calculations/ffh-043-scenario-specialized-wiring.test.ts`

No schema, persistence, profile write, UI redesign, tax percentage/policy, FFH-038, FFH-047, or Phase-7 change.

Worker blocker: NONE.

READY_FOR_MANAGER
