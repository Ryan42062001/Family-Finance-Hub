# FFH-020 — Deploy Accepted FFH-010 / FFH-011 Supabase Migrations

Owner: Application, Data & Integration Engineer
State: ACTIVE
Dependency classification: FFH-024 SATISFIED for staged migration-history recovery; FFH-016 remains blocked until deployment is accepted
Approved integration base: current `phase-5-money-priority-engine`
Execution mode: WORK_MODE_HIGH_VALUE

## Task phase
MANAGER-AUTHORIZED LIVE ENVIRONMENT RECOVERY — STAGE A ONLY

This task previously stopped correctly after discovering pre-existing migration-history drift. FFH-024 has now produced, and Manager has accepted, a bounded recovery sequence.

## Existing verified deployment target
Linked Supabase project: `tsqwvggojeudgspnumze`

Genuinely missing repository migrations/schema:
1. `20260902190000_phase_5a_hybrid_retirement_floor.sql`
2. `20260909005000_ffh_010_hsa_input_contract.sql`
3. `20260909033000_ffh_011_simple_plan_limit_contract.sql`

History-identity drift whose DDL/schema is already present live:
- repository `0001_household_foundation.sql` vs remote-only `20260829180242 household_foundation`;
- repository `20260903134156_phase_5b_goal_intelligence.sql` vs remote-only `20260903135253 phase_5b_goal_intelligence`.

FFH-024 read-only research verified that the remote stored SQL/schema corresponds to those canonical repository migrations. Phase 5A is genuinely absent and must not be repaired as applied.

## Manager authorization — Stage A
One App/Data executor is authorized to perform ONLY the following bounded recovery work using the current supported Supabase CLI against the positively identified linked project:

1. Refresh actual milestone, repository migration files, CLI version/help, linked target identity, migration history, and relevant live schema.
2. Authenticate/link securely. Never expose secrets in repository, chat, committed files, or handoff logs.
3. Preserve a protected pre-change backup plus secret-safe migration-history/schema evidence sufficient to recover/diagnose the pre-change state.
4. History-only canonicalize the foundation migration:
   - mark canonical repository `0001` applied;
   - re-list/verify history;
   - mark remote-only `20260829180242` reverted;
   - re-list/verify history.
5. History-only canonicalize Phase 5B:
   - mark canonical repository `20260903134156` applied;
   - re-list/verify history;
   - mark remote-only `20260903135253` reverted;
   - re-list/verify history.
6. Run `supabase migration list --linked` and prove all historical rows align except the three genuinely pending migrations.
7. Run `supabase db push --linked --include-all --dry-run`.
8. The dry run must show EXACTLY, in this order:
   - `20260902190000_phase_5a_hybrid_retirement_floor.sql`
   - `20260909005000_ffh_010_hsa_input_contract.sql`
   - `20260909033000_ffh_011_simple_plan_limit_contract.sql`
9. STOP and return to Manager with exact secret-safe evidence.

## NOT AUTHORIZED in Stage A
- actual `supabase db push --linked --include-all`;
- any live DDL beyond the history-only repair semantics of the supported CLI `migration repair` flow;
- MCP `apply_migration`;
- manual SQL replay;
- direct edits to `supabase_migrations` tables;
- renaming/editing repository migration files;
- skipping Phase 5A;
- applying/repairing Phase 5A as already applied;
- deploying any FFH-023 legal-marriage migration that may be created on a separate recovery branch.

The real DDL push is a separate Manager gate after Stage A evidence is independently verified.

## Stop conditions
Return `BLOCKED` immediately if:
- CLI/auth/link/backup cannot be established securely;
- current CLI rejects the legacy `0001` repair identity;
- any live history/schema differs from the FFH-024 verified baseline in a material way;
- a repair command produces an unexpected result;
- migration list does not reconcile exactly;
- dry-run includes any migration other than the exact three listed above or changes their order;
- credentials/user action is required but unavailable.

Do not retry destructively. Preserve exact command/version/error and re-inspect read-only state after any failure.

## Acceptance for Stage A
Return `READY_FOR_MANAGER` only when:
- secure backup/pre-change evidence is established;
- both history-identity mismatches are canonically reconciled using supported tooling;
- migration history is re-verified after each repair;
- the exact dry-run pending set is proven;
- no live DDL push was executed.

Update `.ai/engineering/app/HANDOFF.md` and this task with secret-safe evidence.

## Future Stage B
If Manager accepts Stage A, Manager may separately authorize `supabase db push --linked --include-all`, then the original FFH-020 post-deployment schema/RLS/policy/advisor verification. FFH-016 remains blocked until Manager accepts that deployment evidence.

PRODUCTION_SHA: N/A — live environment recovery
VALIDATED_CI: N/A
DEPLOYMENT_TARGET: `tsqwvggojeudgspnumze`
State: ACTIVE — Stage A authorized
