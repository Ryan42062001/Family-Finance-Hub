# FFH-020 — Deploy Accepted FFH-010 / FFH-011 Supabase Migrations

Owner: Application, Data & Integration Engineer
State: ACCEPTED
Dependency classification: FFH-024 SATISFIED for staged migration-history recovery; FFH-016 remains blocked until deployment is accepted
Approved integration base: current `phase-5-money-priority-engine`
Execution mode: WORK_MODE_PREFERRED
Work-mode justification: secure CLI/auth/link/backup setup, migration-history repair, repeated verification, and exact dry-run execution are environment-heavy and benefit materially from autonomous execution.

## Task phase
COMPLETE — MANAGER ACCEPTED LIVE DEPLOYMENT

This task previously stopped correctly after discovering pre-existing migration-history drift. FFH-024 has now produced, and Manager has accepted, a bounded recovery sequence.

## Existing verified deployment target
Linked Supabase project: `tsqwvggojeudgspnumze`

Genuinely missing repository migrations/schema:
1. `20260902190000_phase_5a_hybrid_retirement_floor.sql`
2. `20260909005000_ffh_010_hsa_input_contract.sql`
3. `20260909033000_ffh_011_simple_plan_limit_contract.sql`

History-identity drift whose DDL/schema is already present live:
- repository `0001_household_foundation.sql` vs remote-only `20260829180242 household_foundation`;
- repository `20260903134156_phase_5b_goal_intelligence.sql` vs remote-only `20260903135253 phase_5b_goal_intelligence`.

FFH-024 read-only research verified that the remote stored SQL/schema corresponds to those canonical repository migrations. Phase 5A is genuinely absent and must not be repaired as applied.

## Manager authorization — Stage A
One App/Data executor is authorized to perform ONLY the following bounded recovery work using the current supported Supabase CLI against the positively identified linked project:

1. Refresh actual milestone, repository migration files, CLI version/help, linked target identity, migration history, and relevant live schema.
2. Authenticate/link securely. Never expose secrets in repository, chat, committed files, or handoff logs.
3. Preserve a protected pre-change backup plus secret-safe migration-history/schema evidence sufficient to recover/diagnose the pre-change state.
4. History-only canonicalize the foundation migration:
   - mark canonical repository `0001` applied;
   - re-list/verify history;
   - mark remote-only `20260829180242` reverted;
   - re-list/verify history.
5. History-only canonicalize Phase 5B:
   - mark canonical repository `20260903134156` applied;
   - re-list/verify history;
   - mark remote-only `20260903135253` reverted;
   - re-list/verify history.
6. Run `supabase migration list --linked` and prove all historical rows align except the three genuinely pending migrations.
7. Run `supabase db push --linked --include-all --dry-run`.
8. The dry run must show EXACTLY, in this order:
   - `20260902190000_phase_5a_hybrid_retirement_floor.sql`
   - `20260909005000_ffh_010_hsa_input_contract.sql`
   - `20260909033000_ffh_011_simple_plan_limit_contract.sql`
9. STOP and return to Manager with exact secret-safe evidence.

## NOT AUTHORIZED in Stage A
- actual `supabase db push --linked --include-all`;
- any live DDL beyond the history-only repair semantics of the supported CLI `migration repair` flow;
- MCP `apply_migration`;
- manual SQL replay;
- direct edits to `supabase_migrations` tables;
- renaming/editing repository migration files;
- skipping Phase 5A;
- applying/repairing Phase 5A as already applied;
- deploying any FFH-023 legal-marriage migration that may be created on a separate recovery branch.

The real DDL push is a separate Manager gate after Stage A evidence is independently verified.

## Stop conditions
Return `BLOCKED` immediately if:
- CLI/auth/link/backup cannot be established securely;
- current CLI rejects the legacy `0001` repair identity;
- any live history/schema differs from the FFH-024 verified baseline in a material way;
- a repair command produces an unexpected result;
- migration list does not reconcile exactly;
- dry-run includes any migration other than the exact three listed above or changes their order;
- credentials/user action is required but unavailable.

Do not retry destructively. Preserve exact command/version/error and re-inspect read-only state after any failure.

## Acceptance for Stage A
Return `READY_FOR_MANAGER` only when:
- secure backup/pre-change evidence is established;
- both history-identity mismatches are canonically reconciled using supported tooling;
- migration history is re-verified after each repair;
- the exact dry-run pending set is proven;
- no live DDL push was executed.

Update `.ai/engineering/app/HANDOFF.md` and this task with secret-safe evidence.

## Stage A execution attempt — 2026-09-11

### Repository / authority refresh
- Manager control-plane head independently verified at `6ba3a6980faad82a1f6ca71d6ad1565592f9c0cb` before this documentation update.
- FFH-024 is Manager-accepted recovery authority.
- Canonical migration files refreshed at that head:
  - `0001_household_foundation.sql` blob `52db76f93c60c933f5f420a420d143cb80226d77`;
  - `20260902190000_phase_5a_hybrid_retirement_floor.sql` blob `4bd9c98d9404c8eaa35b213fd7f4a1df6d8cf98c`;
  - `20260903134156_phase_5b_goal_intelligence.sql` blob `2c99e849bb8960732cb898e43e709e9a15b3524f`;
  - `20260909005000_ffh_010_hsa_input_contract.sql` blob `2e2a75a17439c2d75422ec271492333c0eb283c1`;
  - `20260909033000_ffh_011_simple_plan_limit_contract.sql` blob `3f01921b5a346c1e1e95259db30d104e68f8a184`.
- No migration source was edited.

### Live target refresh
Supabase connector read-only verification still matches FFH-024:
- target: `tsqwvggojeudgspnumze` / `Ryan4206's Project`;
- region: `us-east-2`;
- PostgreSQL: `17.6.1.166`;
- status: `ACTIVE_HEALTHY`.

Current remote migration history remains:
1. `20260829180242 household_foundation`
2. `20260829182715 phase_2_household_financial_profile`
3. `20260829183541 phase_2_expenses`
4. `20260829223642 phase_5_ownership_and_planning_foundation`
5. `20260829223716 phase_5_ownership_foundation_indexes`
6. `20260829223935 phase_5_priority_engine_context`
7. `20260829234300 phase_5_tax_profile_context`
8. `20260829234446 phase_5_tax_profile_mfs_context`
9. `20260831235822 phase_5_closure_input_context`
10. `20260901020220 phase_5_adversarial_audit_remediation`
11. `20260903135253 phase_5b_goal_intelligence`

Fresh read-only schema checks confirmed:
- foundation effects remain present: `public.household_role`, `public.households`, `public.household_members`, and `private.is_household_member(uuid)`;
- Phase 5A `expected_hsa_medical_spending_annual` remains absent;
- representative Phase 5B columns remain present: `goal_intelligence_confirmed`, `underlying_need`, `expected_borrowing_apr`;
- FFH-010 profile/month/allocation tables and `retirement_accounts.hsa_ytd_tax_year` remain absent;
- FFH-011 `simple_plan_limit_category` and `simple_plan_limit_tax_year` remain absent.

No material drift from FFH-024 was found.

### Current supported CLI semantics
- Latest non-prerelease upstream Supabase CLI release was verified from the official `supabase/cli` GitHub releases API as `v2.117.0` (published 2026-09-07).
- Current official Supabase documentation still specifies:
  - `supabase migration repair [version] ... [flags]`;
  - `--status applied` inserts/marks a remote history record applied;
  - `--status reverted` removes/marks a remote history record reverted;
  - migration repair changes history only and does not replay/revert SQL;
  - linked `db push --dry-run` previews pending migrations;
  - `--include-all` is required when an older migration is pending behind a later recorded migration.
- Actual local CLI `--version` / `--help` output could not be obtained because this runtime has no Supabase CLI binary.

### Exact runtime capability failure
Pre-write shell preflight:
- `command -v supabase` => no executable found.
- `SUPABASE_ACCESS_TOKEN` => absent.
- `SUPABASE_DB_PASSWORD` => absent.
- `SUPABASE_PROJECT_ID` => absent.
- no cached Supabase CLI configuration/credential files were found under the runtime user's normal Supabase config paths.
- `docker` => no executable found.
- `psql` => no executable found.

Attempts to obtain the CLI without asking the user for credentials:
- `npx --yes supabase@latest --version` => timed out before producing a version/help result.
- `npx --yes supabase@2.117.0 --version` => timed out before producing a version/help result.
- direct shell network checks to `github.com` and `registry.npmjs.org` => `curl: (6) Could not resolve host`.

The chat's Supabase connector is authenticated for supported Supabase tool calls, but that connector session cannot be transferred into the isolated shell/CLI runtime. Stage A expressly forbids substituting MCP `apply_migration` or manual SQL/history edits.

### Backup gate
A protected pre-change CLI backup could not be established because the authorized CLI execution environment is unavailable and the runtime has neither the linked database credentials nor Docker/psql required by the documented Supabase CLI logical-backup flow.

Read-only migration-history and schema evidence was preserved in this task/handoff, but that evidence is not being misrepresented as the required protected recovery backup.

Per the explicit Stage A stop condition, execution stopped here BEFORE any migration-history write.

### Repair commands actually executed
NONE.

The authorized repair commands were not attempted because secure CLI authentication/link and protected backup were not established first.

Therefore there is no after-repair migration-history state to report; the final live history remains the unchanged 11-row pre-change history listed above.

### Final migration list / dry run
- `supabase migration list --linked`: NOT RUN in CLI, because CLI/auth/backup prerequisite failed. The equivalent read-only live history was refreshed through the connected Supabase tool and is listed above.
- `supabase db push --linked --include-all --dry-run`: NOT RUN.
- Exact dry-run output: NOT AVAILABLE because the prerequisite CLI environment could not be established.

### No-write confirmation
- No `supabase migration repair` was run.
- No `supabase db push` of any kind was run.
- No MCP `apply_migration` was used.
- No migration SQL was executed manually.
- No direct `supabase_migrations` write was made.
- No live DDL, data, RLS, configuration, or migration-history change occurred.

### Exact secure action required
Stage A requires a user-controlled or Work/cloud execution environment where the current stable Supabase CLI can run from the repository checkout and authenticate through Supabase's supported secure login/link flow without exposing credentials in chat.

Minimum secure prerequisites:
1. Install current stable Supabase CLI (`v2.117.0` is the current non-prerelease release observed during this attempt) and verify `supabase --version`, `supabase migration list --help`, `supabase migration repair --help`, and `supabase db push --help`.
2. From the refreshed `Ryan42062001/Family-Finance-Hub` checkout at the Manager-approved milestone, run `supabase login` interactively/browser-backed and `supabase link --project-ref tsqwvggojeudgspnumze`. Do not paste the PAT/database password into ChatGPT or commit it.
3. Establish the protected pre-change backup required by FFH-024 using the official Supabase backup flow in that secure environment before any `migration repair` command.
4. Return/restart FFH-020 Stage A in that authenticated environment. The executor must then re-verify history/schema before the first repair; do not pre-run the repair or Stage B push outside the controlled Stage A sequence.

## Future Stage B
If Manager accepts a completed Stage A, Manager may separately authorize `supabase db push --linked --include-all`, then the original FFH-020 post-deployment schema/RLS/policy/advisor verification. FFH-016 remains blocked until Manager accepts that deployment evidence.

PRODUCTION_SHA: N/A — live environment recovery
VALIDATED_CI: N/A
DEPLOYMENT_TARGET: `tsqwvggojeudgspnumze`
DEPLOYMENT_EVIDENCE: Read-only repository/live refresh complete; no live write; CLI/auth/backup prerequisite unavailable in current runtime
State: BLOCKED — Stage A stopped at secure CLI/auth/backup prerequisite


## Stage A completion / Manager reconciliation — 2026-09-18

Stage A is ACCEPTED.

User-controlled Windows execution environment:
- Supabase CLI `2.117.0`;
- Docker Desktop `29.7.2` with healthy daemon;
- linked project `tsqwvggojeudgspnumze`;
- project independently refreshed `ACTIVE_HEALTHY`;
- protected backup created outside the repository before any history write;
- backup set includes roles, schema, data, `supabase_migrations` schema/data, CLI version, Git HEAD, and pre-change migration-list evidence.

To preserve the FFH-020 boundary after later repository integration introduced FFH-023 migration `20260911170000_ffh_023_hsa_legal_spouse_authority.sql`, live recovery uses a detached safe worktree at exact pre-FFH-023 checkpoint:
`945bf8f4403a26812a93a160479cf319096579d5`.

That checkpoint contains the three authorized FFH-020 pending migrations and excludes FFH-023.

History-only recovery completed successfully with supported CLI tooling:
1. `0001 --status applied`;
2. verified both canonical + old alias;
3. `20260829180242 --status reverted`;
4. verified canonical `0001` only;
5. `20260903134156 --status applied`;
6. verified both canonical + old alias;
7. `20260903135253 --status reverted`;
8. verified canonical Phase-5B identity only.

Manager independently refreshed live Supabase history after each pair and confirmed final canonical history includes:
- `0001 household_foundation`;
- all previously aligned historical migrations;
- `20260903134156 phase_5b_goal_intelligence`;
- no `20260829180242`;
- no `20260903135253`.

Fresh read-only schema checks after history repair confirm:
- Phase 5A `expected_hsa_medical_spending_annual` still absent;
- FFH-010 profile/month/allocation tables and `retirement_accounts.hsa_ytd_tax_year` still absent;
- FFH-011 SIMPLE category/year fields still absent;
- representative Phase-5B columns remain present.

Exact dry run from safe worktree:
`npx.cmd --yes supabase@latest db push --linked --include-all --dry-run`

Result: exactly, and only, in this order:
1. `20260902190000_phase_5a_hybrid_retirement_floor.sql`
2. `20260909005000_ffh_010_hsa_input_contract.sql`
3. `20260909033000_ffh_011_simple_plan_limit_contract.sql`

The dry run excludes `0001`, Phase-5B replay, and FFH-023.

No live DDL push has yet been executed.

## Manager authorization — Stage B

Manager now authorizes exactly one live DDL command, from the already-linked safe worktree at `945bf8f4403a26812a93a160479cf319096579d5`:

`npx.cmd --yes supabase@latest db push --linked --include-all`

No other migration source or checkout is authorized for this push.

Required stop/verification sequence after the command:
1. capture exact CLI output;
2. do not retry automatically on any error;
3. run `migration list --linked`;
4. Manager independently refreshes migration history and live schema;
5. verify exact versions `20260902190000`, `20260909005000`, `20260909033000`;
6. verify expected Phase-5A, FFH-010, and FFH-011 schema/RLS/policy effects;
7. run Supabase security/performance advisors;
8. return evidence to Manager before FFH-020 acceptance.

Still NOT AUTHORIZED:
- FFH-023 migration deployment;
- any manual SQL replay;
- direct `supabase_migrations` edits;
- MCP `apply_migration`;
- `db pull`, `db reset --linked`, migration-file edits/renames, or seed deployment;
- any retry after a failed push without Manager diagnosis.

FFH-016 remains blocked until Manager accepts the completed Stage B deployment evidence.


## Stage B deployment / Manager acceptance — 2026-09-18

User executed the exact authorized command from safe worktree checkpoint `945bf8f4403a26812a93a160479cf319096579d5`:

`npx.cmd --yes supabase@latest db push --linked --include-all`

CLI confirmation listed exactly these three migrations, in this order:
1. `20260902190000_phase_5a_hybrid_retirement_floor.sql`
2. `20260909005000_ffh_010_hsa_input_contract.sql`
3. `20260909033000_ffh_011_simple_plan_limit_contract.sql`

CLI output then showed each migration being applied and finished with `Finished supabase db push.`

Manager independently verified live project `tsqwvggojeudgspnumze` after the push:
- project status `ACTIVE_HEALTHY`;
- migration history contains canonical `0001`, canonical Phase-5B `20260903134156`, and newly applied versions `20260902190000`, `20260909005000`, `20260909033000`;
- old aliases `20260829180242` and `20260903135253` remain absent;
- Phase-5A `household_financial_preferences.expected_hsa_medical_spending_annual` is present;
- FFH-010 tables `person_hsa_tax_year_profiles`, `person_hsa_month_statuses`, `household_hsa_married_allocations` are present;
- `retirement_accounts.hsa_ytd_tax_year` is present;
- FFH-011 `retirement_accounts.simple_plan_limit_category` and `simple_plan_limit_tax_year` are present;
- FFH-011 contract constraint `retirement_accounts_simple_plan_limit_contract_check` is present;
- all three FFH-010 tables have RLS enabled;
- all 12 expected HSA read/write policies are present with `private.can_read_household` / `private.can_write_household_financials` predicates;
- authenticated has SELECT/INSERT/UPDATE/DELETE grants; anon has none;
- new HSA tables contain zero rows;
- no existing HSA YTD row was backfilled with `hsa_ytd_tax_year`;
- no existing SIMPLE row was backfilled with category/year;
- no Phase-5A preference row was backfilled with medical-spending input;
- FFH-023 table `household_hsa_legal_spouse_authorities` remains absent, proving that later migration was not accidentally deployed.

Supabase advisors:
- security: zero findings;
- performance: INFO only — two unindexed foreign-key suggestions on `household_hsa_married_allocations` plus unused-index notices on this fresh/low-traffic database.
These are non-blocking performance observations and do not invalidate the deployment contract; no optimization change is authorized under FFH-020.

Protected milestone control-plane CI:
- Manager Stage-B authorization merged at `21afc7d29bf2cde13cdf93b49955a6a416750ae7`;
- Foundation CI #725 / run `35413000748` SUCCESS.

Manager verdict:
**FFH-020 ACCEPTED.**

No financial formula/policy change occurred. No FFH-023 migration was deployed. No manual SQL replay, direct migration-history edit, MCP migration application, reset, pull, seed, or unrelated live refactor occurred.
