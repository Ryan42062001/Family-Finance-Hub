# FFH-020 — Deploy Accepted FFH-010 / FFH-011 Supabase Migrations

Owner: Application, Data & Integration Engineer
State: BLOCKED
Dependency classification: FFH-010 ACCEPTED; FFH-011 ACCEPTED; unlocks blocked FFH-016
Approved integration base: `phase-5-money-priority-engine` at Manager-verified pre-task head `04db9dd7b38480f823c659944c3a3f52924972df` (documentation-only Manager commits may advance the branch without changing deployment scope)
Task branch: DEPLOYMENT-ONLY by default; no production source branch is required unless the accepted migration source itself must change, in which case STOP and return to Manager for separate remediation authorization
Execution mode: WORK_MODE_HIGH_VALUE
Normal-chat fallback: Use ordinary chat with GitHub + linked Supabase tooling, perform the same preflight/deployment/evidence steps sequentially, and stop only on a capability that truly cannot be completed without Work mode.

## Task type

MANAGER-AUTHORIZED LIVE DEPLOYMENT / ENVIRONMENT REMEDIATION

This task exists because FFH-016 correctly proved that the already-accepted FFH-010 and FFH-011 migration files are not applied to the intended linked Supabase project. FFH-020 does not redesign those migrations or re-open their accepted persistence contracts; it deploys the accepted migration source and proves the deployment prerequisite needed to resume FFH-016.

## Objective

Apply the already-accepted FFH-010 HSA and FFH-011 SIMPLE migrations, in repository order, to the intended linked Supabase project `tsqwvggojeudgspnumze`, record reproducible live deployment evidence, and return the environment to Manager so FFH-016 can resume independent PostgREST/RLS/persistence/runtime/browser parity verification.

## Authorized migration source

Apply these exact repository migrations from the current milestone branch, without hand-editing their SQL:

1. `supabase/migrations/20260909005000_ffh_010_hsa_input_contract.sql`
   - repository blob observed by Manager: `2e2a75a17439c2d75422ec271492333c0eb283c1`
   - accepted source contract: FFH-010 / FFH-D005

2. `supabase/migrations/20260909033000_ffh_011_simple_plan_limit_contract.sql`
   - repository blob observed by Manager: `3f01921b5a346c1e1e95259db30d104e68f8a184`
   - accepted source contract: FFH-011

If either migration file differs materially when the worker refreshes, or the target project is not the expected linked environment, STOP and return to Manager rather than deploying stale or ambiguous source.

## Verified starting live evidence

FFH-016 re-verification at branch head `04db9dd7b38480f823c659944c3a3f52924972df` established:

- linked Supabase project `tsqwvggojeudgspnumze` is `ACTIVE_HEALTHY`;
- latest applied migration is `20260903135253 phase_5b_goal_intelligence`;
- migration `20260909005000` is absent;
- migration `20260909033000` is absent;
- `public.person_hsa_tax_year_profiles` is absent;
- `public.person_hsa_month_statuses` is absent;
- `public.household_hsa_married_allocations` is absent;
- `public.retirement_accounts.hsa_ytd_tax_year` is absent;
- `public.retirement_accounts.simple_plan_limit_category` is absent;
- `public.retirement_accounts.simple_plan_limit_tax_year` is absent.

This is the deployment gap FFH-020 is authorized to remediate.

## Required execution sequence

1. Refresh the actual milestone branch and confirm the two authorized migration files still match the accepted source contract.
2. Refresh the linked Supabase project and re-confirm project identity/health, migration history, and the six prerequisite schema objects before making changes.
3. Follow current Supabase migration guidance/tool semantics rather than relying on stale command memory. Use a supported migration mechanism that records proper migration history; do not substitute ad-hoc hand-edited DDL for the repository migrations.
4. Apply `20260909005000_ffh_010_hsa_input_contract.sql` first.
5. Verify that migration `20260909005000` is recorded as applied and that its expected additive HSA schema exists, including RLS being enabled on the three new public tables and the expected policies/grants/constraints being present at the schema level.
6. Apply `20260909033000_ffh_011_simple_plan_limit_contract.sql` second.
7. Verify that migration `20260909033000` is recorded as applied and that the expected SIMPLE columns/constraint/comments exist.
8. Re-list migration history and re-run read-only schema introspection proving all six objects that blocked FFH-016 now exist.
9. Run the available Supabase database/security advisor checks appropriate for these exposed-schema changes and record the result. Do not dismiss a new security finding merely because the migration applied.
10. Update `.ai/tasks/FFH-020.md` and `.ai/engineering/app/HANDOFF.md` with exact, secret-safe deployment evidence and move FFH-020 to `READY_FOR_MANAGER` only when the deployment acceptance criteria are supported.

## Safety boundaries

- No financial-policy changes.
- No Core calculation changes.
- No modifications to the accepted migration SQL during deployment.
- No optimistic backfill of legacy HSA/SIMPLE values.
- No manual data rewrite to manufacture parity evidence.
- No unrelated Supabase refactor, auth change, RLS redesign, grant expansion, configuration change, or schema cleanup.
- Do not expose service-role/secret credentials in repository or handoff evidence.
- Do not perform FFH-016's independent end-to-end parity acceptance inside this task. FFH-020 proves the deployment prerequisite; FFH-016 remains the independent verification gate after Manager reactivation.

## Failure / drift handling

If preflight discovers unexpected live drift (for example, an object exists without the migration-history entry, a conflicting constraint/table definition exists, or additional unapplied dependencies are required), STOP before forcing the migration and return `BLOCKED` with exact evidence.

If either migration application fails, do not repeatedly retry or patch the SQL live. Capture the error and resulting migration/schema state, determine whether the operation was transactional/rolled back from actual evidence, and return `BLOCKED` to Manager for a narrowly scoped remediation decision.

If the migration source itself must change, FFH-020 is no longer deployment-only. Do not edit it under this authorization; request a separate source-remediation task/branch.

## Live execution update — 2026-09-11

### Repository/source preflight

- Refreshed milestone branch: `phase-5-money-priority-engine` at `fb6d19641a490763c15ef3f1eb9f6e3f47934e7a` before FFH-020 documentation writes.
- FFH-010 migration blob is exactly `2e2a75a17439c2d75422ec271492333c0eb283c1` — matches the Manager-authorized source.
- FFH-011 migration blob is exactly `3f01921b5a346c1e1e95259db30d104e68f8a184` — matches the Manager-authorized source.
- No accepted migration SQL was edited.

### Live Supabase preflight

- Target project positively re-identified as `tsqwvggojeudgspnumze` / `Ryan4206's Project` / `us-east-2` / PostgreSQL `17.6.1.166`.
- Project status: `ACTIVE_HEALTHY`.
- `list_migrations` still ends at `20260903135253 phase_5b_goal_intelligence`; FFH-010 `20260909005000` and FFH-011 `20260909033000` remain absent.
- Fresh read-only catalog introspection returned `false` for all six blocked objects/columns:
  - `public.person_hsa_tax_year_profiles`
  - `public.person_hsa_month_statuses`
  - `public.household_hsa_married_allocations`
  - `public.retirement_accounts.hsa_ytd_tax_year`
  - `public.retirement_accounts.simple_plan_limit_category`
  - `public.retirement_accounts.simple_plan_limit_tax_year`
- No unexpected live drift was found at the six-object prerequisite boundary.

### Supported-migration tooling blocker

Current Supabase guidance confirms that remote migration deployment should preserve migration history (`supabase db push` applies pending repository migrations and tracks their migration versions). The connected Supabase MCP `apply_migration` tool is also a supported migration mechanism, but its current schema exposes only `project_id`, `name`, and `query`; it exposes no repository migration `version` parameter.

Current Supabase MCP/Management-API behavior generates the migration version server-side when `apply_migration` is used. Therefore applying either accepted repository file through the currently connected MCP tool would create a new remote-only migration version rather than the repository versions `20260909005000` and `20260909033000`. That would fail FFH-020 acceptance criteria 3/4 and create migration-history drift for future `supabase db push`/`migration list` workflows.

The agent runtime also has no `SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD`, or project-ref credential environment available for invoking the Supabase CLI directly. There is no repository deployment workflow under `.github/workflows/` that can safely execute `supabase db push` using existing repository secrets; only `ci.yml` exists.

Because the task explicitly requires the exact accepted repository migrations to be recorded under their repository versions and forbids ad-hoc history/DDL manipulation, the worker stopped before making any live schema change rather than use MCP `apply_migration` and knowingly create migration-history divergence.

No migration, DDL, data rewrite, policy change, application change, or configuration change was applied during this attempt.

## Acceptance criteria

1. Target environment is positively identified as linked Supabase project `tsqwvggojeudgspnumze` without exposing secrets.
2. Exact accepted migration source is refreshed and unchanged materially before deployment.
3. Migration `20260909005000` is actually applied and present in live migration history.
4. Migration `20260909033000` is actually applied after FFH-010 and present in live migration history.
5. All six schema objects that blocked FFH-016 exist in the live project after deployment.
6. The three FFH-010 public tables have RLS enabled and their expected schema-level policies/grants exist; no deployment step bypasses the accepted household-security contract.
7. Available Supabase advisor checks produce no new unresolved deployment/security blocker attributable to FFH-020, or any finding is explicitly returned to Manager rather than ignored.
8. No unauthorized data/policy/Core/source changes are made.
9. App/Data handoff records reproducible live deployment evidence.
10. Task reaches `READY_FOR_MANAGER`; Manager then independently verifies deployment evidence before reactivating FFH-016.

## Checkpoints

PRODUCTION_SHA: N/A — DEPLOYMENT-ONLY of already-accepted migration source
VALIDATED_CI: N/A unless separate repository remediation is later authorized
HANDOFF_SHA: Not yet established for this blocker update
INTEGRATION_SHA: N/A — no new production source integration expected
DEPLOYMENT_TARGET: Supabase project `tsqwvggojeudgspnumze`
DEPLOYMENT_EVIDENCE: Preflight complete and unchanged; deployment not performed because available MCP migration tool cannot preserve repository migration versions
Validation status: BLOCKED — exact-version history-preserving deployment mechanism is unavailable in the current normal-chat runtime
Escalation count: 0 — capability/tooling boundary, not repeated same-root implementation remediation

## Relationship to FFH-016

FFH-016 remains `BLOCKED` and is not merged into this task. Its downstream PostgREST, role-matrix RLS behavior, null/confirmed persistence/reload, loader -> normalized snapshot, Recommendation Refresh/runtime propagation, and browser capture/reload tests remain intentionally deferred until Manager accepts FFH-020 deployment evidence and explicitly reactivates FFH-016.

## Exact next action

Resume FFH-020 in an execution environment that can run the Supabase CLI against project `tsqwvggojeudgspnumze` using the repository migration directory and authenticated linked-project credentials. From the refreshed milestone checkout, first run a migration-history comparison/dry run, then deploy the pending repository migrations with `supabase db push --linked` (or the equivalent linked-project CLI form) so versions `20260909005000` and `20260909033000` are preserved exactly. Do not use MCP `apply_migration` under the current tool contract because it would generate different remote versions. After the CLI deployment succeeds, return to the Application/Data Engineer to re-list live migration history, verify all six schema objects plus FFH-010 RLS/policies/grants and FFH-011 constraint/comments, run Supabase security/performance advisors, update the task/handoff, and return `READY_FOR_MANAGER` or `BLOCKED` with exact evidence.
