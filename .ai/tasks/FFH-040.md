# FFH-040 — Scenario Overlay + Runner Foundation

Schema: FFH_TASK_V1
Owner: Core Financial Engine Engineer
State: CLOSED
Execution mode: STANDARD_CHAT_HIGH
Refresh mode: FAST_REFRESH
Dependency classification: HARD DEPENDENCY for downstream Scenario Lab UI/integration
Approved integration base: `cc8e2c207f16350c1baeab131ffd685c848948ce`
Assigned branch: `ffh/ffh-040-scenario-runner-foundation`
Activation: WAITING
Blocked by: NONE — task complete
Next owner: Manager / Architect

## Objective

Implement the pure financial-domain foundation for Scenario Lab v1 without creating a second financial engine.

Build a typed, immutable Scenario Definition / overlay / runner layer that applies explicit hypothetical assumptions to an accepted Phase-5 baseline and reruns the existing canonical Money Priority Engine.

This task is deliberately Core-only. It must prove that generic Scenario Lab changes can be represented, validated, applied, and reconciled safely before any authenticated route, server action, page, client state, persistence, or specialized Home/Vehicle/Windfall/Your Plan adapter is built.

Canonical product/technical contract:
`.ai/research/rnd/FFH-039_SCENARIO_LAB_PRODUCT_TECHNICAL_CONTRACT.md`

## Required behavior

### 1. One financial engine

Scenario Lab must reuse `runMoneyPriorityEngine`.

Do not implement a second priority waterfall, second retirement-capacity calculation, second goal competition model, or parallel legal-capacity engine.

The scenario runner should:
1. start from an accepted baseline `MoneyPriorityEngineResult` / normalized snapshot;
2. convert through one authoritative normalized-to-raw representation;
3. apply validated typed scenario overrides to an immutable clone;
4. rerun the existing engine with the same explicit policy/as-of-date contract;
5. return the scenario engine result plus structured scenario provenance/status.

### 2. Extract authoritative normalized-to-raw reuse

The existing `money-priority-hypothetical.ts` contains normalized-snapshot-to-raw conversion needed by accepted hypothetical reruns.

Move/extract that conversion into one reusable authoritative pure module rather than copying it into Scenario Lab.

Requirements:
- existing hypothetical behavior remains regression-equivalent;
- no existing raw/normalized financial field is dropped or widened;
- null/unknown semantics remain exact;
- stable IDs remain stable;
- no display-name-based identity logic is introduced.

### 3. Typed Scenario Definition contract

Create a discriminated, versioned Scenario Definition contract for v1.

It must distinguish:
- recurring financial overrides;
- one-time financial events;
- optional future specialized-module intent boundary without implementing specialized adapters;
- explicit scenario-definition version;
- explicit baseline/fingerprint reference field(s) as opaque provenance only, not authorization.

Do not use arbitrary JSON Patch, unrestricted property paths, or untyped object merge semantics.

Runtime validation must fail closed on:
- unknown override/event types;
- duplicate/conflicting targets;
- malformed/nonfinite money;
- invalid enum/date/boolean values;
- references to missing entities;
- protected legal/statutory fields presented through a generic override;
- ambiguous event combinations that would double-count the same money.

### 4. Generic v1 override support

Implement only generic INCLUDE categories that reuse existing accepted semantics:

- existing income source: amount/gross/active replacement;
- explicitly synthetic/additional income source where meaning and amount are supplied;
- existing recurring expense replacement;
- synthetic recurring expense with explicit essential/cash-flow treatment;
- existing debt: supported balance/payment/APR/state changes;
- atomic cash-funded debt payoff;
- goal target/current/date/user-priority/planned-contribution changes already represented by the snapshot contract;
- existing retirement-account scheduled contribution/planning-input changes that do not change statutory facts;
- person planned-retirement-age change;
- existing insurance-exposure changes already represented by normalized inputs;
- planning preference changes already accepted as non-statutory inputs, including explicit known income disruption/end date where supported;
- generic one-time cash inflow;
- generic one-time cash use;
- cash-impact-only large medical event via ordinary one-time/recurring cash-flow semantics.

Job-loss / parental-leave / temporary-income-reduction scenarios are compositions of explicit income changes plus existing known-disruption/end-date inputs. Do not invent severance, unemployment benefits, leave benefits, taxes, statutory entitlements, or recovery dates.

Childcare is an ordinary user-supplied recurring-expense scenario. Do not add dependent-care tax semantics.

### 5. Protected fields / excluded semantics

Generic Scenario Lab overrides must not create or infer protected legal/statutory facts.

At minimum, do not expose generic mutation of:
- HSA legal-spouse authority;
- HSA month eligibility/coverage/evidence;
- HSA last-month/testing-period facts;
- SIMPLE legal plan-limit category;
- IRA/HSA actual YTD contributions;
- filing-status legal eligibility;
- compensation facts whose product meaning is statutory/legal-capacity authority;
- tax-policy version;
- Money Priority policy version;
- any field whose accepted Phase-5 semantics require separate authority.

Baseline unknown remains unknown unless FFH-039 explicitly classifies the changed field as a safe hypothetical planning input.

### 6. Recurring versus one-time separation

Every scenario operation must have an explicit temporal class.

Preserve:
- recurring monthly income/expense/debt/savings effects;
- one-time cash inflow/use;
- one-time cash-funded payoff;
- one-time cash not becoming recurring income;
- recurring changes not becoming artificial account balances.

Atomic multi-effect events must be represented atomically where separate representation could reuse the same dollars.

For the required cash-funded debt payoff:
- cash consumption and debt principal change are one scenario operation;
- it cannot independently consume cash twice;
- insufficient eligible cash / invalid debt references fail clearly.

### 7. Immutable baseline

No Scenario Lab operation may mutate:
- the baseline normalized snapshot;
- the baseline engine result;
- any nested baseline collection/object;
- accepted policy constants.

Tests must directly prove deep baseline immutability across successful, invalid, and repeated scenario runs.

### 8. Determinism / order behavior

Require:
- repeated equivalent scenario runs produce identical financial outputs;
- independent overrides produce the same result regardless of input order;
- stable entity IDs, not display names, determine targeting;
- duplicate/conflicting overrides fail rather than use last-write-wins;
- scenario-definition ordering does not change cent allocation;
- final-cent behavior remains the canonical engine's behavior.

### 9. Unknown-safe behavior

The runner must preserve `more_information_needed` and existing unknown/legal-capacity behavior.

Do not convert null/unknown to:
- zero;
- false;
- an inferred spouse/account fact;
- invented tax/legal eligibility;
- invented contribution room.

Scenario affordability and legal capacity remain separate concepts.

### 10. Reconciliation

Apply `.ai/shared/FINANCIAL_ENGINE_RECONCILIATION_GATE.md`.

Directly prove at least:
- no-op scenario reproduces baseline engine financially;
- exact aggregate-to-destination equality;
- monthly capacity conservation;
- one-time cash conservation;
- retirement account/owner/shared-group capacity conservation;
- Existing Cash -> Secure -> Build staging remains no-reuse;
- spousal-IRA shared compensation cannot be recreated;
- HSA shared/catch-up capacity cannot be recreated;
- exact annual/monthly and odd-cent behavior remain canonical;
- no epsilon/tolerance waiver;
- no hidden residual clamping.

### 11. Existing hypothetical compatibility

Preserve existing accepted behavior for:
- `runHypotheticalMoneyPriorityEngine`;
- Home affordability;
- Vehicle affordability;
- Windfall;
- Your Plan;
- Recommendation Refresh;
- current Phase-5 tests.

FFH-040 may refactor existing hypothetical plumbing only where needed to establish one authoritative reusable conversion/runner boundary. It must not change accepted financial outcomes merely to simplify Scenario Lab.

### 12. Scenario foundation outputs

The Core implementation should expose a stable pure-domain contract suitable for later App/Data use, such as bounded equivalents of:
- `ScenarioDefinition`;
- scenario override/event discriminated unions;
- structured validation errors/status;
- pure immutable overlay application;
- pure scenario runner;
- baseline/scenario engine results and assumption provenance needed by a later comparison layer.

Exact names/files may differ if the worker proves a cleaner bounded design.

## Explicitly deferred from FFH-040

Do not implement:
- `/scenario-lab` page;
- React/client components;
- server actions/authentication;
- Supabase reads/writes or migrations;
- persisted/saved scenarios;
- localStorage;
- baseline SHA-256 generation / authenticated stale-check orchestration;
- Home adapter;
- Vehicle adapter;
- Windfall adapter;
- Your Plan Scenario Lab layer;
- Recommendation Refresh UI/comparison adapter;
- tax-filing-status scenarios;
- user-selectable investment-return assumptions;
- Monte Carlo/probabilistic simulation;
- dedicated new-child/dependent event;
- dedicated relocation;
- dedicated refinancing;
- HSA/tax-aware medical-event advice;
- FFH-038 work;
- Phase 7.

## Expected bounded production surfaces

Expected new/modified areas:
- `lib/scenarios/**` or a comparably bounded pure-domain directory;
- `lib/calculations/money-priority-hypothetical.ts` only as needed to extract/reuse authoritative conversion behavior;
- directly relevant calculation/scenario tests;
- FFH-040 task/Core handoff evidence.

Do not modify App Router pages/actions or Supabase schema under this task.

## Manager Integration Record

PRODUCTION_SHA: `3407df88440b0742a76cd59aab195c7fe1a103f7`
FINAL_VALIDATION_SHA: `3407df88440b0742a76cd59aab195c7fe1a103f7`
VALIDATED_CI: Foundation CI run `35418840708`, job `105832568156` — SUCCESS; 933/933 calculations; 21/21 security; AI-state validation, dependency audit, typecheck, lint, and build PASS
HANDOFF_SHA: `870967e1cf5b40979e8c959413b35a037e078f90`
INTEGRATION_SHA: `75d2766fb370d506b695d722788b03af5f36a155`
MANAGER_VERDICT: ACCEPTED
AUDIT_STATUS: DEFERRED_TO_FINAL_PHASE6_INTEGRATED_DUAL_AUDIT

## Known CI ownership

- Task-owned: all scenario-foundation implementation/test failures introduced by FFH-040.
- Inherited open production CI debt: NONE.
- FFH-038 remains separate, QUEUED, and must not be pulled into this task.

## Financial reconciliation gate

Applicability: REQUIRED.

Manager acceptance requires independent hand-check of at least one adversarial scenario outside the worker summary, including concrete destinations, ledger consumption, residual, and units.

A reachable cent mismatch, hidden positive residual, tolerance waiver, cross-stage reuse, stale legal-capacity recreation, or aggregate/destination disagreement is remediation-worthy even if total annual statutory capacity remains bounded.

## Validation requirements

Worker must run:
- direct scenario contract/validation tests;
- no-op and immutability tests;
- generic INCLUDE category tests;
- cash-funded payoff no-double-count tests;
- order/repeat determinism tests;
- missing-fact/unknown-safe tests;
- retirement/HSA/spousal-IRA no-reuse adversaries;
- existing hypothetical/Home/Vehicle/Windfall/Your Plan/Refresh regressions materially touched by refactor;
- full calculation suite;
- security tests;
- typecheck;
- lint;
- build;
- Foundation CI on the exact production checkpoint / PR head.

## Acceptance criteria

1. One canonical engine remains authoritative.
2. Scenario Definition is typed/versioned and runtime-validated fail-closed.
3. Baseline is deeply immutable.
4. Generic INCLUDE categories run through accepted engine semantics.
5. Protected legal/statutory fields cannot be generically overridden.
6. Recurring/one-time money cannot be silently conflated.
7. Cash-funded debt payoff is atomic and cannot double-use cash.
8. Unknown legal facts remain unknown.
9. No-op and equivalent input-order scenarios are deterministic.
10. Exact financial reconciliation/no-reuse gate passes.
11. Existing accepted hypothetical and specialized evaluator behavior remains compatible.
12. No UI/App/Data/Supabase/persistence/policy scope crosses into FFH-040.
13. Full Foundation CI succeeds on exact candidate.
14. Worker returns READY_FOR_MANAGER with exact production/test/CI/handoff evidence and the full 11-role dashboard.

## Next action

Core Financial Engine Engineer executes FFH-040 only on the assigned task branch and returns READY_FOR_MANAGER or BLOCKED. Do not self-merge.


## FFH-040 worker completion

Core Financial Engine Engineer completed the bounded Scenario Overlay + Runner Foundation on draft PR #52.

Exact production / final validation checkpoint:
`3407df88440b0742a76cd59aab195c7fe1a103f7`

Foundation CI:
- run `35418840708`;
- job `105832568156`;
- SUCCESS;
- calculations 933/933 PASS;
- security 21/21 PASS;
- AI-state validation PASS;
- production dependency audit PASS;
- typecheck PASS;
- lint PASS with three pre-existing warnings and zero errors;
- production build PASS.

### Implemented foundation

- versioned/discriminated `ScenarioDefinition`;
- strict fail-closed runtime validation with stable-ID targeting;
- duplicate/conflict rejection and protected legal/statutory-field rejection;
- one authoritative `moneyPrioritySnapshotToRaw` adapter shared by existing hypothetical reruns and Scenario Lab;
- deeply immutable scenario overlay output;
- pure `runMoneyPriorityScenario` rerunning only `runMoneyPriorityEngine`;
- structured status, issues, policy basis, baseline reference, and cash/operation provenance;
- generic INCLUDE support for bounded recurring income/expense/debt/goal/retirement-planning/retirement-age/insurance/preference assumptions;
- explicit synthetic income/expense;
- generic one-time cash inflow/use, medical cash-use semantics, goal completion, and atomic cash-funded debt payoff;
- specialized Home/Vehicle/Windfall intent boundary is represented but execution is rejected/deferred as required.

No UI, React, server actions, auth, Supabase, migrations, persistence, localStorage, stale-baseline orchestration, specialized adapter implementation, policy change, FFH-038, or Phase-7 scope was added.

### Direct proof

Focused FFH-040 tests PASS for:
- no-op financial equality with baseline;
- successful-run, invalid-run, and repeated-run baseline immutability;
- protected legal/statutory override rejection;
- duplicate/conflicting target rejection;
- stable-ID targeting despite duplicate display names;
- generic INCLUDE recurring categories;
- repeated/order-independent recurring overrides;
- one-time event order invariance with exact odd-cent `$100.01 - $33.34 = $66.67`;
- recurring versus one-time separation;
- generated scenario account-ID collision rejection;
- atomic `$4,000` cash-funded payoff with one cash consumption and one debt-principal reduction;
- deeply frozen overlay output;
- HSA married-family/shared/catch-up preservation;
- spousal-IRA shared-compensation preservation under no-op and unrelated recurring change;
- missing YTD/plan-compensation legal facts remaining null after income changes.

### Financial Engine Reconciliation Gate

Direct scenario evidence:
- no-op scenario deep-equals the authoritative baseline engine result;
- one-time `$1,000.01` inflow and `$500.00` medical cash use produce exactly `+50,001` cents net cash and do not become monthly income;
- event-order odd-cent control produces exactly `+6,667` cents regardless of input order;
- atomic debt payoff consumes exactly `400,000` cents of eligible cash, reduces principal exactly `400,000` cents, zeros the `$200` recurring minimum, excludes protected/unrelated earmarked cash, and fails closed if eligible cash is insufficient;
- scenario results continue to satisfy the authoritative retirement-capacity invariant;
- HSA test pins shared group `$10,750`, ordinary family room `$8,750`, and two `$1,000` owner catch-ups without recreation under unrelated changes;
- spousal-IRA test preserves the exact `$10,000.01` shared compensation authority under unrelated changes;
- full suite retains FFH-013 M01 exact `$416.67 + $416.66` final-cent routing and the staged Existing Cash / Build / Windfall no-reuse adversary;
- full Phase-5/Home/Vehicle/Windfall/Your Plan/Recommendation Refresh regressions remain green;
- no epsilon/tolerance waiver or hidden positive-residual clamp was introduced.

The first two CI attempts were rejected before final validation:
- `321de7ffc46cb2b6feed62c84dd4d7af1dde2e3b`: one incorrect HSA test expectation;
- `25a40e04c3dbaa34d0bfa7e0f02658aae46110ca`: test-source formatting typo.
A third intermediate `f6cc496593840fd90026d5c55cd7231b7580daa9` passed calculations but exposed security tests that still inspected the old inline adapter location; those tests were moved, without weakening their assertions, to verify the new authoritative adapter. `ff994be486d3cca3a417d8236e35a7394c64aa0d` then passed full CI. The final `3407df88...` checkpoint adds only stronger focused acceptance assertions and also passes full CI.

Worker blocker: NONE.

Manager / Architect now owns independent review of PR #52, the required reconciliation hand-check, acceptance/integration decision, any audit routing, and downstream Phase-6 activation.

READY_FOR_MANAGER


## Manager independent acceptance — 2026-09-18

Manager verdict: **ACCEPTED / CLOSED**.

Verified custody:
- exact production/final-validation SHA `3407df88440b0742a76cd59aab195c7fe1a103f7`;
- full Foundation CI run `35418840708`, job `105832568156` — SUCCESS;
- handoff SHA `870967e1cf5b40979e8c959413b35a037e078f90`;
- handoff-head Foundation CI `35419008620`, job `105833024265` — SUCCESS with predecessor continuity;
- integration SHA `75d2766fb370d506b695d722788b03af5f36a155`;
- merge tree exactly equals tested handoff-head tree.

Manager independently reviewed:
- typed/versioned ScenarioDefinition;
- fail-closed unknown/protected-field validation;
- duplicate/conflict/stable-ID enforcement;
- specialized-intent reject boundary;
- authoritative `moneyPrioritySnapshotToRaw` extraction;
- immutable overlay behavior;
- canonical `runMoneyPriorityEngine` rerun only;
- protected HSA/SIMPLE/YTD/tax/compensation semantics;
- one-time/recurring separation;
- atomic cash-funded payoff;
- HSA/spousal-IRA/shared-capacity preservation;
- existing hypothetical/Home/Vehicle/Windfall/Your Plan/Recommendation Refresh preservation.

Independent reconciliation hand-check:
1. Atomic payoff fixture:
   - eligible payoff cash = $1,000 debt-backed + $5,000 unallocated = $6,000 / 600,000 cents;
   - payoff destination = $4,000 / 400,000 cents;
   - eligible residual = $2,000 / 200,000 cents;
   - protected $9,000 and unrelated goal-earmarked $500 remain untouched;
   - debt principal and minimum payment go to zero;
   - recurring $200/month minimum is released exactly once, not represented as one-time cash.
2. Independent existing-cash control not cited in worker summary:
   - source unallocated cash = $10,000 / 1,000,000 cents;
   - concrete destinations = $4,125 mortgage debt + $4,125 investing;
   - residual liquidity floor = $1,750;
   - exact equality: 1,000,000 = 412,500 + 412,500 + 175,000 cents;
   - FFH-040 no-op deep equality preserves this destination ledger and the retirement-capacity ledger exactly.

No cent mismatch, hidden residual, protected-cash use, legal-capacity recreation, tolerance waiver, second financial engine, or scope crossing was found.

No checkpoint audit is required at FFH-040 alone. The accepted Phase-6 plan retains fresh independent Technical & Mathematical + Financial Policy & Scenario audits for the later frozen integrated Scenario Lab implementation.

FFH-040 is CLOSED / ACCEPTED.
