# FFH-035 — Deploy FFH-023 HSA Legal-Spouse Authority Migration

Owner: Application, Data & Integration Engineer
State: ACTIVE
Dependency classification: FFH-020 ACCEPTED; required before FFH-016 current-milestone runtime/browser parity
Approved integration base: current `phase-5-money-priority-engine`
Execution mode: STANDARD_CHAT_HIGH
Deployment target: `tsqwvggojeudgspnumze`

## Objective

Deploy exactly the already-accepted FFH-023 additive migration:

`supabase/migrations/20260911170000_ffh_023_hsa_legal_spouse_authority.sql`

to the linked Supabase project, then prove migration/schema/RLS/policy/security evidence without changing financial policy, Core behavior, live household data, or any other migration.

## Why this task exists

FFH-020 correctly excluded FFH-023 from its bounded Phase-5A / FFH-010 / FFH-011 recovery source.

After FFH-020 acceptance, the current milestone runtime still queries `public.household_hsa_legal_spouse_authorities` in both the money-priority Supabase loader and HSA profile page. Therefore FFH-016 cannot fairly verify the current milestone until this accepted repository migration is live.

## Current verified starting state

- project: `tsqwvggojeudgspnumze` / `Ryan4206's Project`;
- project status: `ACTIVE_HEALTHY`;
- FFH-020 accepted migrations are live;
- migration history is canonical through `20260909033000`;
- FFH-023 version `20260911170000` is not yet recorded;
- `public.household_hsa_legal_spouse_authorities` is absent;
- current milestone contains the exact accepted migration;
- Supabase security advisor is clean after FFH-020.

## Stage A — preflight / backup / dry run

Authorized now:
1. refresh the local current milestone and confirm the exact migration file is present;
2. verify linked target identity;
3. create a fresh protected pre-change backup outside Git using the same Docker-backed Supabase CLI dump flow used successfully for FFH-020;
4. run `migration list --linked`;
5. verify `20260911170000` is the only repository migration absent remotely;
6. verify the target table is absent;
7. run `supabase db push --linked --dry-run` (or `--include-all --dry-run` only if current CLI requires it);
8. dry run must show exactly one migration:
   `20260911170000_ffh_023_hsa_legal_spouse_authority.sql`;
9. STOP for Manager verification before live DDL.

## Stage A stop conditions

Stop immediately if:
- project identity/status differs;
- backup cannot be established;
- any migration other than `20260911170000` is pending;
- target table already exists unexpectedly;
- dry run lists more than one migration;
- CLI reports history drift or recommends repair;
- any credential or live-state ambiguity appears.

Do not perform a migration-history repair under FFH-035 without new Manager authorization.

## Stage B — not yet authorized

After Manager accepts Stage A, Manager may authorize exactly one live push of the single FFH-023 migration.

Post-push proof must verify:
- migration version `20260911170000` recorded;
- table exists;
- exact expected constraint/index/RLS/policy/grant structure is live;
- no preexisting data was backfilled;
- security advisor remains clean or any finding is explicitly classified;
- performance advisor output is captured;
- no unrelated migration/schema/data change occurred.

## Non-goals

- no edits to migration SQL;
- no FFH-020 rework;
- no financial-policy/Core changes;
- no manual SQL replay;
- no direct migration-history edits;
- no `db reset --linked`;
- no `db pull`;
- no seed deployment;
- no Phase-6 work;
- no PR #5 merge-readiness declaration.

## Downstream gate

When FFH-035 is Manager-accepted, reactivate FFH-016 for the existing verification-only live PostgREST/RLS/persistence/reload/runtime/browser parity gate.

PRODUCTION_SHA: N/A — live deployment task
VALIDATED_CI: N/A — no source implementation change
DEPLOYMENT_TARGET: `tsqwvggojeudgspnumze`
