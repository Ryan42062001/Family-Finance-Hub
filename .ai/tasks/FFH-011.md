# FFH-011 — SIMPLE Persisted-Field Contract

Owner: Application, Data & Integration Engineer
State: REMEDIATION
Dependency classification: independent of FFH-012 HSA behavior; FFH-020 is now BLOCKED on external execution capability, so the App/Data role may take this narrow source/test remediation without waiting for FFH-020 to resume
Approved integration base: `phase-5-money-priority-engine`

## Objective
Maintain an explicit, tax-year-bound SIMPLE plan-limit category contract without silently promoting the legacy ambiguous `simple_higher_limit_eligible` hint.

## Existing accepted checkpoint
PRODUCTION_SHA: `a89e9ae8637f2b5b09a6b4d4736f6b22d119295a`
HANDOFF_SHA: `2ec81b0a3354a066be571ca66796e382753d03c6`
Prior validation: Foundation CI #348 / run `34526162325` reached only calculation tests and was globally red with the then-inherited FFH-012 54-failure set. Security/typecheck/lint/build were skipped and were never proven green at that checkpoint.

## Newly exposed integration failure
After FFH-012 calculation fail-fast cleared, Foundation CI run `34614840278`, job `103314058423`, on milestone integration SHA `b33e97320c8907193ba8f6a571b0d92684237f18` passed calculations and failed at `Test security policy contract`.

The same security stage is still red on the remediated FFH-012 integration SHA `1487b192491a704ca3500b42d22a50289ee1551b` in Foundation CI #404 / run `34624938204` / job `103347645465`, after `Test calculations` passes.

## Manager ownership proof — 2026-09-11
The failing contract is FFH-011-owned validation debt, not FFH-012/FFH-023:
- `tests/security/simple-plan-limit-contract.test.ts` asserts the snapshot source literally contains `simplePlanLimitCategory: nullableString(row.simple_plan_limit_category)`.
- The exact security-test file at accepted FFH-011 SHA `a89e9ae8637f2b5b09a6b4d4736f6b22d119295a` and current integration SHA `1487b192491a704ca3500b42d22a50289ee1551b` has the same blob SHA: `02297242910c554aa9ada8bf099197f20dfbc41e`.
- The actual accepted/current SIMPLE snapshot code normalizes through `const simplePlanLimitCategory = nullableString(row.simple_plan_limit_category) as SimplePlanLimitCategory | null;` and returns `simplePlanLimitCategory` by property shorthand.
- PR #11 does not modify the SIMPLE security test. Its snapshot patch adds HSA spouse-authority behavior and leaves the SIMPLE normalization/return block unchanged.

The regex therefore tests an incidental source-expression shape that the accepted implementation does not use. Classification remains: STALE_OR_OVERLY_TEXTUAL_SECURITY_CONTRACT TEST unless App/Data produces contrary semantic evidence.

## Remediation scope
App/Data must diagnose the exact assertion and make the smallest correction that tests the approved SIMPLE semantic contract rather than an incidental source-code expression shape. Do not weaken the actual guarantees: explicit category, tax-year binding, legacy ambiguity safety, no unsupported inference, persistence/runtime alignment. Do not change SIMPLE statutory formulas, HSA behavior, FFH-015 Core logic, or migration semantics unless evidence proves a real contract defect and Manager expands scope.

Escalation count: this is one narrow owner remediation sequence for the newly exposed FFH-011 validation debt.

## Scheduling
FFH-020 is BLOCKED before any live write because the required secure Supabase CLI/auth/backup environment is unavailable. The App/Data role is therefore available for this narrow FFH-011 remediation in a fresh task-scoped branch/chat. FFH-011 work must not mutate live Supabase state or broaden FFH-020 migration-recovery scope.

This remediation may proceed in parallel with the two FFH-012 independent re-audits because ownership is separate.

## Required validation
Run the exact failing security-contract test, full `npm run test:security`, `npm test`, typecheck, lint, and build when the branch can reach them. Preserve exact failing identities/output. Obtain fresh CI on the integrated milestone path when Manager integrates the remediation.

## Exact next action
Application, Data & Integration Engineer performs one narrow owner remediation iteration in a fresh task-scoped branch, updates this task and App/Data handoff, and returns `READY_FOR_MANAGER` or `BLOCKED`. Do not self-accept.