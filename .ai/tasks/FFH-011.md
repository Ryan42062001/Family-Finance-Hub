# FFH-011 — SIMPLE Persisted-Field Contract

Owner: Application, Data & Integration Engineer
State: READY_FOR_MANAGER
Dependency classification: INDEPENDENT of FFH-012 after FFH-010 acceptance
Approved integration base: `phase-5-money-priority-engine` (grandfathered shared-branch Workflow V2 exception)
Task branch: Shared-branch exception because work was already in flight before Workflow V2 adoption

## Objective

Resolve what `simple_higher_limit_eligible` / `simpleHigherLimitEligible` safely means prospectively and for legacy data so Core Engineering can later consume the field without silently applying verified higher SIMPLE statutory treatment to an ambiguous category.

## Required behavior

- inspect schema/migration history, UI/actions, snapshot loader/normalizer, and all current uses;
- make prospective persisted semantics explicit and non-ambiguous;
- preserve legacy/unknown safety and require reconfirmation where existing values cannot prove statutory category;
- preserve user-editable/correctable behavior and tax-year/version context where materially required;
- keep persistence/runtime naming and semantics aligned;
- add focused persistence/application/contract tests;
- distinguish source migration changes from actual linked Supabase deployment/runtime parity.

## Non-goals

- no Core SIMPLE statutory formula change;
- no FFH-012 HSA legal-capacity implementation or repair;
- no FFH-013 R6/spousal-IRA implementation;
- no Phase 5C implementation;
- no financial-policy invention.

## Acceptance criteria

1. Persisted SIMPLE category semantics are explicit for prospective data.
2. Legacy ambiguous data cannot automatically become affirmative statutory evidence.
3. Runtime/snapshot contract matches persistence.
4. No unsupported statutory inference is introduced.
5. FFH-011-owned focused/regression validation is green and inherited FFH-012 red state is explicitly isolated rather than repaired by App/Data.
6. Exact production and validation checkpoints are recorded.
7. Current App/Data handoff is updated to FFH-011 and state becomes `READY_FOR_MANAGER` before Manager review.

## Owned systems / overlap risk

Primary ownership: persistence/schema, application capture/actions, loaders/normalization, related tests.

Known concurrent overlap risk: FFH-012 Core HSA work was already committed on the same milestone branch before FFH-011. Branch-level CI therefore contains inherited Core failures. App/Data must not repair FFH-012 merely because its commit is at branch head.

## Checkpoints

PRODUCTION_SHA: `a89e9ae8637f2b5b09a6b4d4736f6b22d119295a` (`test: isolate FFH-011 SIMPLE runtime regression fixture`)
VALIDATED_CI: Foundation CI #348 / run `34526162325` / calculation job `103035463838` on exact `PRODUCTION_SHA` — workflow FAILURE because 54 inherited FFH-012 HSA calculation failures remain; post-remediation calculation summary is 810 tests / 756 pass / 54 fail / 0 skipped / 0 cancelled
HANDOFF_SHA: `2ec81b0a3354a066be571ca66796e382753d03c6` (`docs: hand off FFH-011 remediation`)
INTEGRATION_SHA: Not yet established

Validation status: SCOPED FFH-011 REGRESSION REMEDIATED / READY_FOR_MANAGER; branch-level CI remains red only at the Manager-isolated FFH-012 calculation gate
Escalation count: 1 owner remediation iteration under Workflow V2

## Remediation performed

The isolated regression was traced to `lib/calculations/money-priority-advanced-retirement.test.ts`. Its Core-formula regression fixture still supplied deprecated persisted input `simple_higher_limit_eligible`, while FFH-011 intentionally makes that legacy ambiguous field non-authoritative.

The focused remediation changed only that test fixture so it supplies the existing runtime compatibility boolean directly. This preserves the intended Core formula regression coverage without restoring the legacy persisted boolean as statutory evidence and without implementing FFH-015 Core SIMPLE formula remediation.

No production persistence, migration, loader/normalizer semantics, HSA behavior, or Core retirement formula was changed by this remediation iteration.

## Failure ownership / validation evidence

Manager-established pre-remediation attribution was:

- 54 calculation failures already existed on FFH-012 checkpoint `98f9090b5a231cb12a0f68d3be7e84c2bbf4f546` / Foundation CI #308 and are inherited/out of FFH-011 scope;
- FFH-011 added exactly one incremental failure: `SIMPLE age 40 higher=true uses plan-specific limit`;
- Foundation CI #309 on original FFH-011 candidate `f85f7779a6bebac87d433289212f8671b46d8212` therefore had the inherited 54 plus the one incremental SIMPLE regression.

Post-remediation Foundation CI #348 ran on exact checkpoint `a89e9ae8637f2b5b09a6b4d4736f6b22d119295a` and reported exactly 54 calculation failures: 810 tests, 756 passed, 54 failed, 0 skipped, 0 cancelled. The remediation commit changed only the isolated SIMPLE regression fixture, and the total failure count returned from the Manager-isolated 55 set to the inherited 54-failure FFH-012 baseline. No additional FFH-011-owned regression is demonstrated by this evidence.

Observed CI steps that passed before the calculation gate:
- checkout/setup;
- dependency installation;
- production dependency audit.

The workflow stopped after calculation tests returned exit code 1 because the 54 inherited FFH-012 failures remain. Security policy-contract tests, TypeScript typecheck, lint, and production build were therefore not reached and are not claimed as passes for run #348.

## Blocking issues

No remaining blocker is demonstrated inside the authorized FFH-011 remediation scope.

The 54 remaining HSA calculation failures are Manager-attributed to FFH-012 and are explicitly out of App/Data scope. They must not be repaired under FFH-011 merely to make branch-level CI green.

## Unverified items

- Live linked Supabase deployment/runtime parity for the FFH-011 SIMPLE migration remains unverified and is not claimed by this source-level task.
- Downstream security/typecheck/lint/build were not reached on Foundation CI #348 because inherited FFH-012 calculation failures trigger fail-fast behavior.
- Manager acceptance, `TASK_INDEX.md` refresh, and any integration checkpoint remain Manager-owned.

## Exact next action

Manager / Architect independently verifies `PRODUCTION_SHA` `a89e9ae8637f2b5b09a6b4d4736f6b22d119295a`, Foundation CI #348 scoped failure-attribution evidence, and `HANDOFF_SHA` `2ec81b0a3354a066be571ca66796e382753d03c6`. If accepted, Manager updates the Manager-owned task index/canonical integration state and may unblock/schedule FFH-015 in a collision-safe Core slot. If Manager identifies a concrete FFH-011-owned regression, return FFH-011 to remediation with that evidence.
