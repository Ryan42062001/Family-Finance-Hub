# FFH-011 — SIMPLE Persisted-Field Contract

Owner: Application, Data & Integration Engineer
State: REMEDIATION
Dependency classification: independent of FFH-012 HSA behavior; same-role scheduling must avoid collision with active FFH-020 deployment work
Approved integration base: `phase-5-money-priority-engine`

## Objective
Maintain an explicit, tax-year-bound SIMPLE plan-limit category contract without silently promoting the legacy ambiguous `simple_higher_limit_eligible` hint.

## Existing accepted checkpoint
PRODUCTION_SHA: `a89e9ae8637f2b5b09a6b4d4736f6b22d119295a`
HANDOFF_SHA: `2ec81b0a3354a066be571ca66796e382753d03c6`
Prior validation: Foundation CI #348 / run `34526162325` reached only calculation tests and was globally red with the inherited FFH-012 54-failure set. Security/typecheck/lint/build were skipped and were never proven green at that checkpoint.

## Newly exposed integration failure — 2026-09-11
After FFH-012 remediation allowed Foundation CI to progress, run `34614840278`, job `103314058423`, on milestone integration SHA `b33e97320c8907193ba8f6a571b0d92684237f18` passed calculations and then failed at `Test security policy contract`.

Manager isolated a concrete FFH-011 textual-contract mismatch. `tests/security/simple-plan-limit-contract.test.ts` asserts that the snapshot source literally contains `simplePlanLimitCategory: nullableString(row.simple_plan_limit_category)`. The actual accepted snapshot code creates `const simplePlanLimitCategory = nullableString(row.simple_plan_limit_category) as SimplePlanLimitCategory | null;` and later returns `simplePlanLimitCategory` via property shorthand. That exact implementation shape was already present at accepted FFH-011 checkpoint `a89e9ae8637f2b5b09a6b4d4736f6b22d119295a`.

PR #8 / FFH-012 did not modify the SIMPLE snapshot or the SIMPLE security-contract test. The failure is therefore FFH-011-owned validation debt newly exposed after the inherited calculation fail-fast was removed.

Classification: STALE_OR_OVERLY_TEXTUAL_SECURITY_CONTRACT TEST unless App/Data produces contrary semantic evidence.
Escalation count: 1 prior owner-remediation iteration; this newly exposed root cause is distinct and starts its own owner remediation sequence.

## Remediation scope
App/Data must diagnose the exact security assertion and make the smallest correction that tests the approved SIMPLE semantic contract rather than an incidental source-code expression shape. Do not weaken the actual guarantees: explicit category, tax-year binding, legacy ambiguity safety, no unsupported inference, persistence/runtime alignment. Do not change SIMPLE statutory formulas, HSA behavior, FFH-015 Core logic, or migration semantics unless evidence proves a real contract defect and Manager expands scope.

## Scheduling
FFH-020 is currently ACTIVE under the same App/Data role and owns live deployment of accepted FFH-010/011 migrations. Avoid simultaneous edits to the shared App/Data handoff. Unless Manager explicitly reschedules, finish/return the FFH-020 event first, then execute this narrow FFH-011 remediation in a fresh task-scoped App/Data chat/branch.

## Required validation
Run the exact failing security-contract test, full `npm run test:security`, `npm test`, typecheck, lint, and build when the branch can reach them. Preserve exact failing identities/output. Obtain fresh CI on the integrated milestone path when Manager integrates the remediation.

## Exact next action
Application, Data & Integration Engineer performs one narrow owner remediation iteration after the current FFH-020 event, updates this task and App/Data handoff, and returns `READY_FOR_MANAGER` or `BLOCKED`. Do not self-accept.