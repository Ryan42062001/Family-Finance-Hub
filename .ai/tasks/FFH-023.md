# FFH-023 — FFH-012 Audit Remediation

Owner: Work Helper / Super Troubleshooter
State: ACTIVE
Dependency classification: cent/reconciliation defects are independent and may begin immediately; spouse-identity implementation is SOFT-BLOCKED on FFH-022 Manager-approved authority
Approved integration base: current `phase-5-money-priority-engine`
Execution mode: WORK_MODE_HIGH_VALUE
Fallback: normal high-effort chat with repository/test/CI tooling is valid.

## Objective
Remediate the independently confirmed FFH-012 technical/accounting defects without changing approved financial policy.

## Audit findings to remediate
1. MEDIUM blocking — partial-year married-family equal allocation can create a phantom cent. Example: shared ordinary base `$5,104.17` may become owner ceilings `$2,552.09 + $2,552.09 = $5,104.18`.
2. MEDIUM blocking — Build can report `$729.17` aggregate monthly HSA/retirement allocation while routing only `$729.16`, because the PR #8 `> 0.01` residual exception silently tolerates one unreconciled cent.
3. LOW non-blocking — married ledger component fields named as remaining shared/catch-up room can remain stale after consumption.
4. LOW non-blocking — `withNormalizedHsaFacts()` can overwrite intentionally canonical partial/unknown test facts if reused carelessly.
5. HIGH blocking protected-semantic finding — `spouse_partner` is ambiguous and must not be reinterpreted here. FFH-022 determines the authority contract. Do not implement a marriage discriminator until Manager approves that result.

## Required method
- Refresh actual milestone/task/audit evidence first.
- Read `.ai/roles/work-helper.md`, `.ai/shared/WORK_HELPER_OVERLAY.md`, `.ai/work-helper/PLAYBOOK.md`, FFH-012, FFH-022, both audit handoffs/reports, FFH-D005, and only code/tests required.
- Reproduce each technical cent defect before editing.
- Capture exact failing/reproduction evidence, expected/actual values, and affected code path.
- Implement deterministic cent-safe behavior so legal capacity is never increased by rounding and aggregate allocations reconcile exactly to routed account allocations or explicitly expose an unresolved remainder.
- Remove the one-cent Build escape hatch as a silent accounting workaround.
- Add adversarial regressions for odd-cent partial-year married sharing and `$8,750 / $4,375 + $4,375` monthly reconciliation.
- Address LOW findings only if the correction is narrow and does not expand scope.
- Do not infer legal marriage from `spouse_partner`, filing status, or any new field until FFH-022 is Manager-approved.

## Validation
At minimum run:
- focused new regression tests;
- FFH-012 HSA legal-capacity tests;
- retirement-capacity / Build / integration tests affected;
- full calculation suite;
- any repository CI-equivalent checks available.

Preserve exact failed-test identities and before/after evidence. Do not claim CI success without exact-SHA proof.

## Branch / handoff
Use a clean recovery branch from the refreshed milestone, preferably `ffh/ffh-023-ffh012-audit-remediation`.
Update `.ai/work-helper/HANDOFF.md` and this task.
Return exactly one:
- `READY_FOR_MANAGER_VERIFICATION`
- `BLOCKED`
- `ESCALATION_REQUIRED`

Do not mark ACCEPTED, AUDIT_READY, or CLOSED. Do not merge production work.
