# FFH-023 — FFH-012 Audit Remediation

Owner: Work Helper / Super Troubleshooter
State: ACTIVE
Dependency classification: FFH-022 SATISFIED; complete the integrated FFH-012 remediation candidate
Approved integration base: current `phase-5-money-priority-engine`
Recovery branch: `ffh/ffh-023-ffh012-audit-remediation`
PR: #11 — keep open; do not merge until complete remediation is Manager-verified
Execution mode: WORK_MODE_HIGH_VALUE
Fallback: normal high-effort chat with repository/test/CI tooling is valid.

## Objective
Complete all blocking FFH-012 audit remediation under the now-accepted FFH-022 legal-marriage authority while preserving the already-demonstrated cent/reconciliation fixes.

## Already completed technical recovery
Production checkpoint `f0439bae550aa5243cf8928cc05c9a41b8406956` on the recovery branch fixes:
1. odd-cent partial-year married-family equal allocation without creating legal capacity;
2. exact Build aggregate-to-account monthly reconciliation without a one-cent escape hatch.

Before/after evidence:
- `$5,104.17` shared base previously became `$2,552.09 + $2,552.09 = $5,104.18`; candidate now produces `$2,552.08 + $2,552.09 = $5,104.17`.
- Build previously reported `$729.17` while routing `$729.16`; candidate now reports/routes `$729.16` and leaves the remaining need unresolved.

Local evidence on that checkpoint: focused 2/2; affected suites 40/40; full calculations 794/794. Exact-SHA CI is not yet established. Inherited FFH-011 security/typecheck debt remains separate.

## Manager-accepted protected semantics — FFH-022
Policy authority is now integrated via PR #12 / milestone commit `07e42e531309e10cb7d1127fb022362d4d5f942b` and `.ai/policy/retirement/FFH-022_HSA_LEGAL_MARRIAGE_AUTHORITY_POLICY.md`.

Mandatory semantics:
- legal-spouse authority is pair-specific and target-HSA-tax-year-bound;
- minimum state is affirmative legal spouses / confirmed non-spouses / unknown;
- `spouse_partner` alone never authorizes married HSA sharing;
- planning `tax_filing_status` is corroborating only and never creates/erases spouse authority;
- married-allocation data is preference only after affirmative authority;
- unknown/missing stays unknown-safe with targeted `more_information_needed` where spouse status is material;
- confirmed non-spouses are evaluated independently under existing person/month HSA rules;
- no silent prior-year carryforward.

## Newly authorized implementation scope
Work Helper is explicitly authorized to complete the cross-layer technical implementation of the accepted FFH-022 contract, including the smallest coherent changes required across:
- additive persistence/schema migration(s);
- normalized snapshot/loader/types;
- HSA legal-capacity evaluation;
- Existing Cash / Secure / Build / Windfall / Your Plan / hypothetical reruns / Recommendation Refresh propagation;
- UI capture/validation needed to distinguish legal spouse / non-spouse / unknown for the target tax year;
- tests and fixtures.

This is technical implementation authority only. Do not derive marriage from effective-date history, state-law status, filing status, or `spouse_partner`; those derivations are not authorized.

### Migration/deployment sequencing boundary
You MAY create the additive repository migration required for the new legal-spouse authority contract on the FFH-023 branch.
You MUST NOT deploy it live or alter current Supabase migration history.
FFH-020 is separately repairing existing live migration history and proving the current pending set from the milestone. Keep FFH-023's new migration isolated on the recovery branch until Manager decides integration/deployment sequencing.

## Required regression scenarios
At minimum implement/test the FFH-022 scenarios:
1. affirmative legal spouses + full-year family coverage -> one shared `$8,750` ordinary base and equal `$4,375/$4,375` default;
2. confirmed non-spouse partners -> no married shared group/equal split;
3. unknown/missing authority with material family coverage -> targeted `more_information_needed`;
4. MFJ planning status + unknown -> still unresolved;
5. `single` planning status + affirmative spouse authority -> filing status cannot disable spouse sharing;
6. MFJ planning status + confirmed non-spouse -> filing status cannot create sharing;
7. married-allocation row + unknown/non-spouse -> cannot create authority;
8. unknown-safe locality for supported self-only/unrelated routes;
9. mixed material case where only spouse-dependent HSA capacity blocks;
10. tax-year isolation;
11. pair/person order invariance;
12. authority change / Recommendation Refresh invalidates stale shared-family recommendations.

Preserve/re-run existing month sensitivity, Medicare, last-month treatment, YTD/tax-year isolation, catch-up, multiple-HSA, cent-safe allocation, exact reconciliation, and cross-consumer tests.

## Branch handling
Refresh the live milestone before continuing. Reconcile/rebase the existing FFH-023 branch onto the current milestone while preserving `f0439bae...` behavior and PR #11 history. Do not create a competing PR unless the existing branch cannot safely be reconciled; if that occurs, return to Manager.

## Validation
Run targeted new regressions, all affected FFH-012/retirement suites, full `npm test`, security, typecheck, lint, and build. Preserve exact failure identities. Classify inherited failures rather than hiding them. Obtain exact-SHA CI if the repository/PR trigger supports it; do not claim CI success without proof.

## Handoff
Update `.ai/work-helper/HANDOFF.md` and this task. Return exactly one:
- `READY_FOR_MANAGER_VERIFICATION`
- `BLOCKED`
- `ESCALATION_REQUIRED`

Report production SHA separately from documentation/handoff SHA. Do not mark ACCEPTED/AUDIT_READY/CLOSED and do not merge PR #11.
