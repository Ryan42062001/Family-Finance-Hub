# FFH-016 — Live Supabase Migration and Runtime Parity Verification

Owner: Application, Data & Integration Engineer
State: CLOSED
Dependency classification: FFH-010 source contract ACCEPTED; FFH-011 SIMPLE contract ACCEPTED; FFH-020 live deployment ACCEPTED; FFH-035 legal-spouse migration deployment ACCEPTED; runtime parity prerequisites SATISFIED
Approved integration base: `phase-5-money-priority-engine` at pre-activation head `8f56a68a9ab018e15f0e32b11df140e04c4db615` (Manager documentation may advance the branch without changing this verification scope)
Task branch: VERIFICATION-ONLY by default; no production branch required unless remediation is separately authorized
Execution mode when reactivated: WORK_MODE_PREFERRED
Work-mode justification: live Supabase/PostgREST/RLS/browser parity verification requires sustained environment interaction and repeated runtime checks; policy/reasoning work remains Standard Chat High.
Standard Chat High fallback: Continue with repository + linked Supabase evidence in ordinary chat; complete every database/PostgREST/RLS/runtime check available there and explicitly preserve any browser-only remainder instead of blocking the whole task.

## Task type

VERIFICATION-ONLY PRE-MERGE READINESS GATE

This task may complete without a production code commit. Workflow checkpoint semantics for this task are based on concrete runtime/deployment evidence rather than an invented production SHA.

## Objective

Prove that accepted repository migration/data contracts are actually applied and behave correctly in the linked/live Supabase-backed application environment before Phase 5 is declared merge-ready.

## Required verification after reactivation

For accepted in-scope migrations/contracts:
- verify migration is actually applied to the intended linked Supabase project/environment;
- verify expected tables/columns/enums/constraints are available through the runtime path;
- verify PostgREST selection/write behavior used by the application;
- verify household-scoped RLS behavior for representative owner/member/viewer/nonmember or the canonical role matrix, using safe/rollback-capable methods;
- verify null/unknown semantics survive persistence -> loader -> normalized snapshot -> reload;
- verify browser/end-to-end capture and reload where the feature has UI capture;
- verify Recommendation Refresh/runtime input propagation where materially applicable;
- distinguish repository source existence from live deployment evidence.

## Active scope

- FFH-010 migration `supabase/migrations/20260909005000_ffh_010_hsa_input_contract.sql` and accepted HSA contract.
- FFH-011 migration `supabase/migrations/20260909033000_ffh_011_simple_plan_limit_contract.sql` and Manager-accepted SIMPLE persistence/runtime contract at `a89e9ae8637f2b5b09a6b4d4736f6b22d119295a`.

## Non-goals

- no financial-policy changes;
- no Core calculation changes;
- no optimistic inference that a migration file was deployed;
- no unrelated Supabase refactor;
- no silent production fix if live evidence reveals a mismatch; route remediation to Manager first.

## Acceptance criteria

1. Target Supabase environment/project is identified without exposing secrets.
2. Actual applied schema/migration state is evidenced for FFH-010 and FFH-011.
3. Required PostgREST/persistence paths work against live migrated schema.
4. RLS role behavior is evidenced safely.
5. Null/unknown and confirmed-value semantics survive persistence -> loader -> normalized snapshot -> reload.
6. Browser/runtime capture and reload are verified where applicable, or any unavailable browser-only check is explicitly isolated with the strongest reproducible normal-chat evidence completed first.
7. Any mismatch is routed as a separate remediation rather than silently fixed outside authorization.
8. Task reaches READY_FOR_MANAGER with reproducible evidence and current App/Data handoff.

## Checkpoints

PRODUCTION_SHA: N/A — VERIFICATION-ONLY; no production code/schema remediation authorized
VALIDATED_CI: N/A — live verification task; no remediation CI claimed
HANDOFF_SHA: `04ffc2675bcf23f6a3a12b140071149bfda94de5` — FFH-016 live parity handoff on `ffh/ffh-016-live-runtime-parity-verification`
INTEGRATION_SHA: N/A — no remediation integrated

Runtime evidence: READY_FOR_MANAGER. Final live verification on 2026-09-18 used milestone `b8e60292a1f13fb66f0c055f2c3f4e110bd380f9` and linked project `tsqwvggojeudgspnumze`. Live migration/schema parity, exact application selector compatibility, rollback-only authenticated RLS/write semantics, HSA/SIMPLE/legal-spouse persistence, normalized runtime propagation, and Recommendation Refresh source/test propagation passed within the Standard Chat High lane. No persistent live data mutation remains.

Validation status: READY_FOR_MANAGER — no production/schema mismatch found. Direct authenticated browser/PostgREST HTTP capture is isolated as the permitted environment-only remainder because the live project contains zero auth users/households and persistent fixture creation is outside the rollback-only verification boundary.
Escalation count: 0

## Sequencing notes

- FFH-011 is Manager-accepted, so HSA and SIMPLE live parity should be verified together in one controlled session after deployment.
- This task is independent of FFH-012 Core HSA remediation because it verifies persistence/runtime contract deployment rather than Core legal-capacity formulas.
- A live schema/runtime mismatch was found. Verification correctly stopped at that boundary; no migration was silently applied and no production remediation was performed.
- Manager has now authorized separate task FFH-020 to deploy only the exact accepted FFH-010 then FFH-011 migrations to the intended linked project and prove deployment/schema/security evidence.
- FFH-016 must remain BLOCKED while FFH-020 is ACTIVE. Do not run a second App/Data chat for FFH-016 in parallel with the deployment task.
- FFH-020 deployment evidence is not itself FFH-016 parity acceptance. Manager must independently accept FFH-020 and explicitly reactivate this task before downstream checks resume.

## Re-verification — 2026-09-11

- Linked project `tsqwvggojeudgspnumze` reported `ACTIVE_HEALTHY`, region `us-east-2`, PostgreSQL `17.6.1.166`.
- Fresh `list_migrations` ended at `20260903135253 phase_5b_goal_intelligence`; versions `20260909005000` and `20260909033000` were absent.
- Fresh read-only catalog query returned `false` for all six required objects: the three FFH-010 HSA tables and the three FFH-010/FFH-011 `retirement_accounts` columns.
- Migration application and expected schema/API availability therefore fail. PostgREST write/read, household RLS role behavior, persistence/reload, normalized runtime propagation, Recommendation Refresh, and browser capture/reload remain prerequisite-blocked and are not claimed.
- No DDL, migration application, data write, runtime change, or repair was performed.

## Exact next action

Remain BLOCKED while Application, Data & Integration Engineering executes Manager-authorized FFH-020. When FFH-020 reaches `READY_FOR_MANAGER`, Manager independently verifies that the exact accepted migrations are applied to project `tsqwvggojeudgspnumze` with the required migration/schema/security evidence. Only after Manager accepts FFH-020 should FFH-016 be reactivated to resume PostgREST, safe RLS matrix, persistence -> loader -> normalized snapshot -> reload, Recommendation Refresh/runtime propagation, and browser capture/reload verification.


## Manager sequencing update — 2026-09-18

FFH-020 is now Manager-accepted: FFH-010/011 and Phase-5A are live with verified schema/RLS/policy/security evidence.

FFH-016 remains BLOCKED for one newly explicit runtime-parity dependency, not because FFH-020 failed.

Current milestone code now queries `public.household_hsa_legal_spouse_authorities` in both:
- `lib/supabase/money-priority-snapshot.ts`;
- `app/financial-profile/hsa/page.tsx`.

That accepted FFH-023 table is still repository-only and intentionally was not deployed under FFH-020. Starting current-milestone browser/runtime parity before it is live would create a known schema mismatch unrelated to FFH-010/011 correctness.

Manager therefore routed FFH-035 as the narrow deployment gate for exactly:
`20260911170000_ffh_023_hsa_legal_spouse_authority.sql`.

Exact next action:
remain BLOCKED until Manager accepts FFH-035 deployment evidence, then reactivate FFH-016 for its existing verification-only PostgREST/RLS/persistence/reload/normalized-runtime/Recommendation Refresh/browser parity scope.


## Manager reactivation — 2026-09-18

FFH-035 is Manager-accepted. The current milestone's complete required Supabase migration set is now live on `tsqwvggojeudgspnumze`, including:
- Phase-5A hybrid retirement-floor input;
- FFH-010 HSA input contract;
- FFH-011 SIMPLE persisted-field contract;
- FFH-023 HSA legal-spouse authority.

FFH-016 is now ACTIVE as a verification-only pre-merge readiness gate.

Authorized scope:
- verify live PostgREST selection/write behavior used by current milestone code;
- verify safe household RLS role behavior;
- verify null/unknown and confirmed-value persistence -> loader -> normalized snapshot -> reload;
- verify SIMPLE persisted-field behavior;
- verify HSA legal-spouse authority persistence/runtime path;
- verify Recommendation Refresh/runtime propagation where applicable;
- verify browser capture/reload where available;
- do not silently remediate any mismatch; route a separate task if a production defect is found.

No production/schema mutation is authorized by this reactivation.


## Final live parity verification — 2026-09-18

Exact baseline:
- Manager milestone / PR #5 head: `b8e60292a1f13fb66f0c055f2c3f4e110bd380f9`;
- linked project: `tsqwvggojeudgspnumze`;
- project status: `ACTIVE_HEALTHY`;
- PostgreSQL: `17.6.1.166`.

PASS evidence:
- live migration history contains Phase-5A `20260902190000`, FFH-010 `20260909005000`, FFH-011 `20260909033000`, and FFH-023 `20260911170000`;
- exact current loader/HSA/SIMPLE select lists parse against live schema;
- required constraints/RLS/policies/grants are live;
- rollback-only owner/member/viewer/nonmember matrix passed;
- HSA YTD null -> confirmed, expected HSA spending null -> confirmed, HSA profile/month unknown -> confirmed, married allocation, legal-spouse unknown -> confirmed, and SIMPLE null -> category/year all round-tripped;
- explicit rollback completed and post-test live counts returned to zero for every synthetic row;
- loader/normalizer source preserves HSA/SIMPLE/legal-spouse unknown/confirmed semantics;
- Recommendation Refresh fingerprints the complete normalized snapshot and has direct accepted HSA propagation coverage;
- security advisor reports zero findings; performance findings are INFO-only;
- current milestone Foundation CI run `35413944471` / #731 is SUCCESS with docs-only predecessor continuity.

Environment-only remainder:
- direct authenticated PostgREST HTTP/browser capture is unavailable in this Standard Chat connector;
- the live project has zero auth users and zero household rows;
- persistent test-fixture creation solely for browser capture is outside the safe rollback-only authority;
- this remainder is explicitly isolated under the task's Standard Chat fallback and does not represent an observed production mismatch.

PRODUCTION_SHA: N/A — verification-only; no production/schema remediation
VALIDATED_CI: N/A — no remediation CI claimed; milestone CI context recorded above
HANDOFF_SHA: `04ffc2675bcf23f6a3a12b140071149bfda94de5`
INTEGRATION_SHA: N/A — Manager-owned
MANAGER_VERDICT: ACCEPTED
AUDIT_STATUS: NOT_REQUIRED — FFH-016 is verification-only; final integrated Phase-5 audits are separate merge-readiness tasks FFH-036 / FFH-037

## Exact next action after FFH-016 worker completion

Manager independently verifies the live evidence and this handoff. If accepted, route the final integrated Phase-5 Technical + Financial Policy audit/review, then refresh PR #5 status/description and perform final merge review. Do not declare PR #5 merge ready from this worker result.


## Manager independent acceptance — 2026-09-18

Manager verdict: **ACCEPTED / CLOSED**.

Manager independently verified the worker result against exact frozen production/milestone target `b8e60292a1f13fb66f0c055f2c3f4e110bd380f9`, verification branch `ffh/ffh-016-live-runtime-parity-verification`, HANDOFF_SHA `04ffc2675bcf23f6a3a12b140071149bfda94de5`, and final evidence head `1ea8efb088ea19037a08699fe0e9c13fee6e6df6`.

Independent repository review:
- worker branch is four commits ahead of the frozen target and changes only `.ai` verification/control-plane documentation;
- no production, calculation, migration, application, package, or schema source changed under FFH-016;
- PR #5 remains open with exact head `b8e60292a1f13fb66f0c055f2c3f4e110bd380f9`;
- every static `supabase-js` select list used by the frozen money-priority loader, HSA page, and SIMPLE page was compared with live `information_schema.columns`; no selected column is missing.

Independent live Supabase review:
- project `tsqwvggojeudgspnumze` is `ACTIVE_HEALTHY`;
- canonical migration history includes Phase-5A, FFH-010, FFH-011, and FFH-023;
- live project began with zero auth users, households, financial rows, and HSA rows;
- RLS helper definitions independently match the role contract: any household member may read; only `owner` / `member` may write; owner-only checks remain distinct;
- relevant financial/HSA tables independently show the expected authenticated read/write policies.

Manager independently reproduced the rollback-only role/persistence matrix in one live transaction using the production household-creation trigger and authenticated JWT-claim semantics. All checks passed:
- owner read/write;
- member read/write;
- viewer read with update filtered to zero and insert denied by RLS;
- nonmember read zero, update zero, and insert denied by RLS;
- HSA YTD `NULL/NULL/NULL -> 1000/250/2026`;
- expected HSA medical spending `NULL -> 1800`;
- HSA profile `unknown/unknown -> not_elected/not_applicable`;
- twelve HSA month rows `unknown/unknown/unknown -> eligible/family/confirmed`;
- married allocation `0/0 -> 4000/4750`;
- legal-spouse authority `unknown -> confirmed_legal_spouses`;
- SIMPLE contract `NULL/NULL -> standard/2026`.

The transaction ended in explicit `ROLLBACK`. Fresh post-test counts independently returned to zero for auth users, households, memberships, people, preferences, retirement accounts, HSA profiles/months/allocations, and legal-spouse authority rows.

Runtime-source review independently confirmed:
- live HSA/SIMPLE/legal-spouse fields flow through `loadMoneyPrioritySnapshot` into normalization;
- HSA YTD tax year and expected HSA medical spending remain nullable;
- legal-spouse authority preserves explicit unknown/confirmed states;
- SIMPLE category/year remain explicit and tax-year-bound; the ambiguous legacy boolean is not promoted to affirmative authority;
- direct accepted tests show HSA decision-basis changes alter Recommendation Refresh financial fingerprints and cannot remain `current`;
- HSA and SIMPLE hypothetical reruns preserve their normalized contracts.

Direct authenticated browser/PostgREST HTTP capture remains unavailable because this Standard Chat lane has no browser-auth session and the live project has zero persistent auth/household fixtures. Creating persistent fixtures solely for transport capture would exceed the rollback-only live-data boundary. Acceptance criterion 6 explicitly permits isolating this unavailable browser-only remainder after the strongest reproducible normal-chat evidence is completed. Manager therefore classifies it as **ENVIRONMENT-ONLY / NON-BLOCKING**, not an observed production mismatch.

Fresh Supabase security advisor: zero findings. Performance advisor: INFO-only unindexed-FK / unused-index observations; no performance remediation is authorized by FFH-016.

No separate FFH-016 remediation is warranted.

FFH-016 is CLOSED. This does **not** make PR #5 merge-ready. Manager routes the final integrated Phase-5 Technical & Mathematical audit and Financial Policy & Scenario audit as FFH-036 and FFH-037 against the unchanged frozen target `b8e60292a1f13fb66f0c055f2c3f4e110bd380f9`.
