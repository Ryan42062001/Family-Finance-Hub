# FFH-016 — Live Supabase Migration and Runtime Parity Verification

Owner: Application, Data & Integration Engineer
State: ACTIVE
Dependency classification: FFH-010 source contract ACCEPTED; FFH-011 SIMPLE contract ACCEPTED
Approved integration base: `phase-5-money-priority-engine` at pre-activation head `8f56a68a9ab018e15f0e32b11df140e04c4db615` (Manager documentation may advance the branch without changing this verification scope)
Task branch: VERIFICATION-ONLY by default; no production branch required unless remediation is separately authorized
Execution mode: WORK_MODE_HIGH_VALUE
Normal-chat fallback: Continue with repository + linked Supabase evidence in ordinary chat; complete every database/PostgREST/RLS/runtime check available there and explicitly preserve any browser-only remainder instead of blocking the whole task.

## Task type

VERIFICATION-ONLY PRE-MERGE READINESS GATE

This task may complete without a production code commit. Workflow V2 checkpoint semantics for this task are based on concrete runtime/deployment evidence rather than an invented production SHA.

## Objective

Prove that accepted repository migration/data contracts are actually applied and behave correctly in the linked/live Supabase-backed application environment before Phase 5 is declared merge-ready.

## Required verification

For accepted in-scope migrations/contracts:
- verify migration is actually applied to the intended linked Supabase project/environment;
- verify expected tables/columns/enums/constraints are available through the runtime path;
- verify PostgREST selection/write behavior used by the application;
- verify household-scoped RLS behavior for representative owner/member/viewer/nonmember or the canonical role matrix, using safe/rollback-capable methods;
- verify null/unknown semantics survive persistence -> loader -> normalized snapshot -> reload;
- verify browser/end-to-end capture and reload where the feature has UI capture;
- verify Recommendation Refresh/runtime input propagation where materially applicable;
- distinguish repository source existence from live deployment evidence.

## Active scope

- FFH-010 migration `supabase/migrations/20260909005000_ffh_010_hsa_input_contract.sql` and accepted HSA contract.
- FFH-011 migration `supabase/migrations/20260909033000_ffh_011_simple_plan_limit_contract.sql` and Manager-accepted SIMPLE persistence/runtime contract at `a89e9ae8637f2b5b09a6b4d4736f6b22d119295a`.

## Non-goals

- no financial-policy changes;
- no Core calculation changes;
- no optimistic inference that a migration file was deployed;
- no unrelated Supabase refactor;
- no silent production fix if live evidence reveals a mismatch; route remediation to Manager first.

## Acceptance criteria

1. Target Supabase environment/project is identified without exposing secrets.
2. Actual applied schema/migration state is evidenced for FFH-010 and FFH-011.
3. Required PostgREST/persistence paths work against live migrated schema.
4. RLS role behavior is evidenced safely.
5. Null/unknown and confirmed-value semantics survive persistence -> loader -> normalized snapshot -> reload.
6. Browser/runtime capture and reload are verified where applicable, or any unavailable browser-only check is explicitly isolated with the strongest reproducible normal-chat evidence completed first.
7. Any mismatch is routed as a separate remediation rather than silently fixed outside authorization.
8. Task reaches READY_FOR_MANAGER with reproducible evidence and current App/Data handoff.

## Checkpoints

PRODUCTION_SHA: N/A — VERIFICATION-ONLY
VALIDATED_CI: N/A unless repository remediation is separately authorized; record live evidence instead
HANDOFF_SHA: Not yet established
INTEGRATION_SHA: N/A unless remediation code is later integrated

Runtime evidence: Not yet established
Validation status: ACTIVE / NOT YET VERIFIED
Escalation count: 0

## Sequencing notes

- FFH-011 is Manager-accepted, so HSA and SIMPLE live parity should be verified together in one controlled session where practical.
- This task is independent of FFH-012 Core HSA remediation because it verifies persistence/runtime contract deployment rather than Core legal-capacity formulas.
- If live schema/runtime mismatch requires code or migration changes, stop verification at the mismatch, record evidence, and return to Manager for a remediation task/authorization.

## Exact next action

Refresh the live branch and linked Supabase project, identify the intended environment without exposing secrets, then verify FFH-010 HSA and FFH-011 SIMPLE migration application, schema/API availability, safe RLS behavior, persistence/null semantics, normalized runtime propagation, and browser capture/reload where available. Record exact reproducible evidence, update the App/Data handoff, and move to `READY_FOR_MANAGER` only when the acceptance criteria are supported.
